/-
This file was generated by Aristotle (https://aristotle.harmonic.fun).

Lean version: leanprover/lean4:v4.24.0
Mathlib version: f897ebcf72cd16f89ab4577d0c826cd14afaafc7
This project request had uuid: abaf75d9-1352-41eb-a8a5-71ec9408f90c

To cite Aristotle, tag @Aristotle-Harmonic on GitHub PRs/issues, and add as co-author to commits:
Co-authored-by: Aristotle (Harmonic) <aristotle-harmonic@harmonic.fun>
-/

/-
We have formally defined the sieve setting, including the set of primes $\mathcal{P}_n$,
the primorial $q$, the permitted residue classes, and the survivor indicator function $f$.
We have defined the Discrete Fourier Transform $\hat{f}$ and the physical variance
$\sigma^2$.

We have proven the following main results:
- Parseval's Identity for the Sieve (`parseval_identity`): The variance of the indicator
  function is equal to the sum of the squared magnitudes of its non-zero Fourier
  coefficients: $\sigma^2 = \sum_{h \ne 0} |\hat{f}(h)|^2$.
- Harmonic Variance Lower Bound (`harmonic_variance_lower_bound`): For any non-zero
  frequency $h$, the variance is bounded below by twice the squared magnitude of the
  Fourier coefficient at $h$: $\sigma^2 \ge 2 |\hat{f}(h)|^2$.

These proofs rely on the orthogonality of characters on $\mathbb{Z}/q\mathbb{Z}$ and
the conjugate symmetry of the Fourier transform for real-valued functions, which we have
also formally established.

Definitions:
- `r_K`: The Krafft tuple.
- `A_n`: The target interval of indices.
- `survives_iff`: Characterization of survival in the sieve.

Theorems:
- `krafft_algebraic_equivalence`: Equivalence between modular condition and
  divisibility by 6x ± 1.
- `interval_projection_bound`: Bound on 6x+1 for x in A_n.
- `sieve_isomorphism`: x survives the sieve iff 6x-1 and 6x+1 are prime.

We have successfully formalized the additive variance sieve (the Turán method) by defining
the local hit function, the global hit counter, and the first and second moments over the
target interval. We have also proved the Additive Sieve Isomorphism.

Definitions:
- `g`: The local hit function $g_i(x)$.
- `c`: The global additive hit counter $c(x)$.
- `M_1`: The First Moment $M_1(n)$.
- `M_2`: The Second Moment $M_2(n)$.

Theorems:
- `additive_sieve_isomorphism`: Proves that $c(x) = 0$ if and only if $x$ survives the
  Krafft sieve (i.e., $6x-1$ and $6x+1$ are prime).
 
Formal verification of the Additive Moments Expansion phase.

This module contains the formal statements and proofs (or admitted bounds as permitted)
for the moments of the additive hit counter.

Key results:
- `first_moment_expansion`: verifying the swap of summation order for the First Moment.
- `second_moment_expansion`: verifying the expansion and swap of summation order for the
  Second Moment.
- `local_hit_density_bound`: bounding the local hits in the interval A_n.
- `cross_term_density_bound`: bounding the simultaneous hits for distinct primes in the
  interval A_n.

Helper lemmas `count_mod_bound`, `crt_intersection_bound`, and `cross_term_splitting`
were formalized and proved to support the proof of `cross_term_density_bound`.

Formal verification of the Additive Moments Evaluation phase.

This module defines the Prime Reciprocal Sum H(n) and establishes upper bounds for the
First and Second Moments of the additive hit counter.

Key definitions and results:
- `H`: The sum of the reciprocals of the primes in the sieve window.
- `first_moment_bound`: Proves that the First Moment M_1(n) is bounded by 2L * H(n) + 2w.
- `second_moment_bound`: Proves that the Second Moment M_2(n) is bounded by
  4L * H(n)^2 + 4w^2 + M_1(n).

These bounds are derived using the Local Hit Density Bound and the Cross-Term Density
Bound established in Phase 2.
 
We have formalized the Weighted Sieve Principle, including the definitions of the weighted
interval mass $S_1(n)$ and the weighted hit mass $S_2(n)$. We proved Lemma 4.4 (Non-negative
Hits) showing that the hit counter $c(x)$ is non-negative and integer-valued (in the sense
that $c(x) < 1 \implies c(x) = 0$). Finally, we proved Theorem 4.5 (The Weighted Existence
Principle), which states that if $S_2(n) < S_1(n)$, then there exists a survivor
$x \in \mathcal{A}_n$ with positive weight $W(x) > 0$ and zero hits $c(x) = 0$.

Formal verification of the Weighted Sieve Principle phase.

Formal verification of the Fourier Expansion of the Weighted Moments phase.

We have successfully transitioned the weighted moments into the frequency domain using
the compact support of $W(x)$ and Plancherel's theorem.

Key Definitions and Lemmas:
- `W_hat`: The Discrete Fourier Transform of the weight function $W(x)$.
- `g_hat`: The Discrete Fourier Transform of the local hit function $g_i(x)$.
- `compact_support_equivalence` (Lemma 5.3): Proves that $S_1(n) = q \hat{W}(0)$ given
  the compact support of $W$.
- `plancherel_hit_expansion` (Lemma 5.4): Expands the weighted hit count $S_2(n)$ into
  the Fourier domain as $S_2(n) = q \sum_{i, h} \hat{W}(h) \overline{\hat{g}_i(h)}$.

Helper Lemmas:
- `sum_W_eq_S1`: Relates the sum of $W$ over the full space to the sum over the interval
  $\mathcal{A}_n$.
- `sum_exp_orthogonality_neg`: Establishes the orthogonality of the exponential sum with
  a negative sign.
- `plancherel_theorem_custom`: Proves Plancherel's theorem for the specific Fourier
  transform definition used.
 
Formalization of Phase 6 of the Resonant Frequency Collapse.

We have formalized the sparse, discrete Fourier spectrum of the local hit functions.
- Lemma 6.1 (The Dirac Comb of the Hit Function) is split into `dirac_comb_nonzero` and
  `dirac_comb_zero`.
- Lemma 6.2 (The Zero-Frequency Main Term) is formalized as `zero_frequency_main_term`.
- Theorem 6.3 (The Resonant Sieve Equation) is formalized as `resonant_sieve_equation`.
 
Formalization of Phase 7: The Final Evaluation.

This module formalizes the final evaluation of the analytic bounds for the Twin Prime
survivor count. It includes:
- Lemma 7.1: Bounds the local exponential sum and its product over all primes.
- Lemma 7.2: Defines the statement of the Dirichlet Sine Envelope bound (admitted as an
  assumption).
- Theorem 7.3: Establishes the final error bound by combining Lemma 7.1 and Lemma 7.2.
- The final lower bound for the survivor count S, expressing the strict positivity
  requirement.
-/

import Mathlib.Algebra.Group.Pointwise.Finset.Basic
import Mathlib.Analysis.Complex.Exponential
import Mathlib.Analysis.SpecialFunctions.Complex.Log
import Mathlib.Analysis.SpecialFunctions.Trigonometric.Basic
import Mathlib.CategoryTheory.Category.Basic
import Mathlib.Data.Finset.Sort
import Mathlib.Data.Nat.Basic
import Mathlib.Data.Nat.Nth
import Mathlib.Data.Nat.Prime.Basic
import Mathlib.Data.Real.Basic
import Mathlib.Data.ZMod.QuotientRing
import Mathlib.NumberTheory.ArithmeticFunction
import Mathlib.NumberTheory.Bertrand
import Mathlib.NumberTheory.PrimeCounting
import Mathlib.Tactic

set_option linter.mathlibStandardSet false

open scoped BigOperators
open scoped Real
open scoped Nat
open scoped Classical
open scoped Pointwise

set_option maxHeartbeats 0
set_option maxRecDepth 4000
set_option synthInstance.maxHeartbeats 20000
set_option synthInstance.maxSize 128

set_option relaxedAutoImplicit false
set_option autoImplicit false

noncomputable section

/--
#### Definition of the set of primes P_n.
Let $\mathcal{P}_n$ denote the set of primes $p$ such that $5 \le p < 6n+2$.
--/
def P_n (n : ℕ) : Finset ℕ := (Finset.range (6 * n + 2)).filter (fun p => 5 ≤ p ∧ p.Prime)

/--
#### Definition of the primorial q.
Define the primorial $q = \prod_{p \in \mathcal{P}_n} p$.
--/
def q (n : ℕ) : ℕ := (P_n n).prod (fun p => p)

/--
#### Definition of w as the cardinality of P_n.
Let $w = |\mathcal{P}_n|$ be the number of distinct prime factors of $q$.
--/
def w (n : ℕ) : ℕ := (P_n n).card

/--
#### Definition of the sorted list of primes and the accessor function p_i.
Index the primes in $\mathcal{P}_n$ as $p_1, p_2, \dots, p_w$.
--/
def primes_list (n : ℕ) : List ℕ := (P_n n).sort (· ≤ ·)

/--
Access the $i$-th prime $p_i$. Note that we use 0-based indexing for the implementation,
so $p_0$ corresponds to the user's $p_1$.
--/
def p (n : ℕ) (i : Fin (w n)) : ℕ := (primes_list n).get (i.cast (by
unfold w primes_list; aesop;))

/--
#### Definition of valid residues r.
For each $p_i$, let $r_i$ be an integer satisfying $0 < r_i \le \lfloor p_i/2 \rfloor$.
--/
def valid_r (n : ℕ) (r : Fin (w n) → ℕ) : Prop :=
  ∀ i, 0 < r i ∧ r i ≤ (p n i) / 2

/--
#### Definition of the permitted residue classes A_i.
Define the set of permitted residue classes for each prime as
$A_i = (\mathbb{Z}/p_i\mathbb{Z}) \setminus \{\pm r_i \pmod{p_i}\}$.
--/
def A_i (n : ℕ) (r : Fin (w n) → ℕ) (i : Fin (w n)) : Finset (ZMod (p n i)) :=
  haveI : NeZero (p n i) := ⟨by
  -- Since $p n i$ is an element of $P_n n$, and $P_n n$ consists of primes greater than
  --or equal to 5, $p n i$ must be at least 5. Hence, it cannot be zero.
  have h_prime : ∀ i : Fin (w n), 5 ≤ p n i := by
    intro i
    have h_prime : p n i ∈ P_n n := by
      -- Since the sorted list is a permutation of the original set, every element in the
      -- sorted list is in the original set.
      have h_perm : ∀ x ∈ primes_list n, x ∈ P_n n := by
        exact fun x hx => Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 hx;
      exact h_perm _ ( List.get_mem _ _ )
    exact (Finset.mem_filter.mp h_prime).right.left
  exact ne_of_gt (by linarith [h_prime i])⟩
  Finset.univ.filter (fun x => x ≠ (r i : ZMod (p n i)) ∧ x ≠ -(r i : ZMod (p n i)))

/--
#### Definition of the global set of surviving residues A.
Define the global set of surviving residues $A \subseteq \mathbb{Z}/q\mathbb{Z}$ such that
$x \in A$ if and only if $x \pmod{p_i} \in A_i$ for all $1 \le i \le w$.
--/
def A (n : ℕ) (r : Fin (w n) → ℕ) : Finset (ZMod (q n)) :=
  haveI : NeZero (q n) := ⟨by
  exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2⟩
  Finset.univ.filter (fun x => ∀ i : Fin (w n), (x.cast : ZMod (p n i)) ∈ A_i n r i)

/--
#### Definition of the indicator function f.
Define the indicator function $f : \mathbb{Z}/q\mathbb{Z} \to \mathbb{R}$ such that
$f(x) = 1$ if $x \in A$, and $f(x) = 0$ otherwise.
--/
noncomputable def f (n : ℕ) (r : Fin (w n) → ℕ) (x : ZMod (q n)) : ℝ :=
  if x ∈ A n r then 1 else 0

/--
#### Definition of N as the cardinality of A.
Let $N = |A| = \prod_{i=1}^w (p_i - 2)$ be the total number of surviving residues.
--/
def N (n : ℕ) (r : Fin (w n) → ℕ) : ℕ := (A n r).card

/--
#### Definition of the global mean density μ.
Define the global mean density $\mu = \frac{N}{q}$.
--/
noncomputable def μ (n : ℕ) (r : Fin (w n) → ℕ) : ℝ := (N n r : ℝ) / (q n : ℝ)

/--
#### Definition of the Discrete Fourier Transform f_hat.
Define the Discrete Fourier Transform of $f$ at frequency $h \in \mathbb{Z}/q\mathbb{Z}$ as:
  $$ \hat{f}(h) = \frac{1}{q} \sum_{x=0}^{q-1} f(x) e^{-2\pi i h x / q} $$
--/
noncomputable def f_hat (n : ℕ) (r : Fin (w n) → ℕ) (h : ZMod (q n)) : ℂ :=
  haveI : NeZero (q n) := ⟨by
    exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2⟩
  (1 / (q n : ℂ)) * ∑ x : ZMod (q n), (f n r x) * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ))

/--
#### Definition of the physical variance σ_sq.
Define the physical variance of the survivor distribution as:
  $$ \sigma^2 = \frac{1}{q} \sum_{x=0}^{q-1} (f(x) - \mu)^2 $$
--/
noncomputable def σ_sq (n : ℕ) (r : Fin (w n) → ℕ) : ℝ :=
  haveI : NeZero (q n) := ⟨by
    exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2⟩
  (1 / (q n : ℝ)) * ∑ x : ZMod (q n), (f n r x - μ n r) ^ 2

/-
Instance proof that q is non-zero.
-/
instance q_ne_zero (n : ℕ) : NeZero (q n) := ⟨by
  apply Finset.prod_ne_zero_iff.mpr
  intro p hp
  have hp_prime : p.Prime := (Finset.mem_filter.mp hp).2.2
  exact hp_prime.ne_zero⟩

/-
Lemma: The Fourier coefficient at 0 is equal to the mean density μ.
-/
theorem f_hat_zero_eq_mu (n : ℕ) (r : Fin (w n) → ℕ) :
    f_hat n r 0 = μ n r := by
  simp [f_hat, μ];
  rw [ inv_mul_eq_div, div_eq_div_iff ] <;> norm_cast <;> norm_num [ q_ne_zero ];
  · unfold f N; aesop;
  · exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2;
  · exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2

/-
Lemma: Orthogonality of the exponential sum.
-/
lemma sum_exp_orthogonality (n : ℕ) (k : ZMod (q n)) :
    ∑ h : ZMod (q n), Complex.exp (2 * Real.pi * Complex.I * h.val * k.val / (q n : ℂ)) = if k = 0 then (q n : ℂ) else 0 := by
  erw [ ← Finset.sum_subset ( Finset.subset_univ ( Finset.image ( fun x : ℕ => ↑x : ℕ → ZMod ( q n ) ) ( Finset.range ( q n ) ) ) ) ];
  · rw [ Finset.sum_image ] <;> norm_num;
    · split_ifs;
      · rw [ Finset.sum_congr rfl fun x hx => by rw [ Nat.mod_eq_of_lt ( Finset.mem_range.mp hx ) ] ] ; aesop;
      · -- Let $z = e^{2 \pi i k / q}$. Since $k \neq 0$, $z$ is a primitive $q$-th root of unity.
        set z : ℂ := Complex.exp (2 * Real.pi * Complex.I * k.val / (q n : ℂ))
        have hz : z ≠ 1 := by
          rw [ Ne.eq_def, Complex.exp_eq_one_iff ];
          field_simp;
          rintro ⟨ m, hm ⟩;
          rw [ div_eq_iff ] at hm <;> norm_cast at * <;> simp_all;
          · replace hm := congr_arg ( fun x : ℤ => x : ℤ → ZMod ( q n ) ) hm ; simp_all;
          · exact Nat.ne_of_gt <| Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2;
        -- The sum of a geometric series with ratio $z$ is zero when $z \neq 1$.
        have h_geo_series : ∑ x ∈ Finset.range (q n), z ^ x = 0 := by
          rw [ geom_sum_eq ] <;> norm_num [ hz ];
          exact Or.inl ( by rw [ ← Complex.exp_nat_mul, mul_comm, Complex.exp_eq_one_iff.mpr ⟨ k.val, by push_cast; ring_nf; norm_num [ show q n ≠ 0 from Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2 ] ⟩ ] ; ring );
        convert h_geo_series using 2 ; ring_nf;
        rw [ ← Complex.exp_nat_mul ] ; rw [ Nat.mod_eq_of_lt ( Finset.mem_range.mp ‹_› ) ] ; ring_nf;
        simp +decide [ mul_assoc, mul_comm, mul_left_comm ];
    · exact fun x hx y hy hxy => Nat.mod_eq_of_lt hx.out ▸ Nat.mod_eq_of_lt hy.out ▸ by simpa [ ZMod.natCast_eq_natCast_iff ] using hxy;
  · simp +zetaDelta at *;
    intro x; exact ⟨ x.val, by exact x.val_lt, by exact ZMod.natCast_zmod_val x ⟩ ;

/-
Orthogonality of the exponential sum over x.
-/
lemma orthogonality_x (n : ℕ) (k : ZMod (q n)) :
    ∑ x : ZMod (q n), Complex.exp (2 * Real.pi * Complex.I * k.val * x.val / (q n : ℂ)) = if k = 0 then (q n : ℂ) else 0 := by
      convert sum_exp_orthogonality n k using 1;
      rw [ ← Equiv.sum_comp ( Equiv.ofBijective ( fun x : ZMod ( q n ) => x ) ⟨ fun x y hxy => by aesop, fun x => ⟨ x, by aesop ⟩ ⟩ ) ] ; norm_num [ mul_assoc, mul_comm, mul_left_comm ]

/-
Application of orthogonality to simplify the double sum of function values.
-/
lemma sum_orthogonality_application (n : ℕ) (r : Fin (w n) → ℕ) :
    ∑ x : ZMod (q n), ∑ y : ZMod (q n), (f n r x : ℂ) * (f n r y : ℂ) *
    ∑ h : ZMod (q n), Complex.exp (2 * Real.pi * Complex.I * h.val * (y - x).val / (q n : ℂ)) =
    (q n : ℂ) * ∑ x : ZMod (q n), (f n r x : ℂ)^2 := by
      -- Apply the orthogonality result to simplify the inner sum over $h$.
      have h_inner : ∀ x y : ZMod (q n), ∑ h : ZMod (q n), Complex.exp (2 * Real.pi * Complex.I * h.val * (y - x).val / (q n : ℂ)) = if y = x then (q n : ℂ) else 0 := by
        intro x y; split_ifs with h; simp_all;
        have := sum_exp_orthogonality n ( y - x ) ; simp_all +decide [ sub_eq_iff_eq_add ] ;
      simp_all +decide [ sq, mul_assoc, mul_comm, Finset.mul_sum _ _ _ ]

/-
Expansion of the squared magnitude of a single Fourier coefficient.
-/
lemma f_hat_normSq_expansion (n : ℕ) (r : Fin (w n) → ℕ) (h : ZMod (q n)) :
    Complex.normSq (f_hat n r h) =
    (1 / (q n : ℂ)^2) * ∑ x : ZMod (q n), ∑ y : ZMod (q n), (f n r x : ℂ) * (f n r y : ℂ) *
    Complex.exp (2 * Real.pi * Complex.I * h.val * (y - x).val / (q n : ℂ)) := by
      -- By definition of $f_hat$, we have:
      have h_def : f_hat n r h = (1 / (q n : ℂ)) * ∑ x : ZMod (q n), (f n r x : ℂ) * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ)) := by
        exact rfl;
      -- By definition of $f_hat$, we have $\overline{\hat{f}(h)} = \frac{1}{q} \sum_y f(y) e^{2\pi i h y / q}$.
      have h_conj : starRingEnd ℂ (f_hat n r h) = (1 / (q n : ℂ)) * ∑ y : ZMod (q n), (f n r y : ℂ) * Complex.exp (2 * Real.pi * Complex.I * (h.val * y.val : ℕ) / (q n : ℂ)) := by
        simp_all +decide [ Complex.ext_iff, Complex.exp_re, Complex.exp_im ];
        norm_num [ neg_div, mul_div_assoc, Real.cos_neg, Real.sin_neg ];
        have h_exp : ∀ x : ZMod (q n), Complex.re (h.val * x.val) = h.val * x.val ∧ Complex.im (h.val * x.val) = 0 := by
          norm_cast ; aesop;
        aesop;
      convert congr_arg₂ ( · * · ) h_def h_conj using 1 <;> ring_nf;
      · norm_num [ Complex.mul_conj, Complex.normSq_eq_norm_sq ];
      · -- By combining the exponents, we can see that the left-hand side and right-hand side are equal.
        have h_exp : ∀ x y : ZMod (q n), Complex.exp ((q n : ℂ)⁻¹ * Real.pi * Complex.I * h.val * (y - x).val * 2) = Complex.exp ((q n : ℂ)⁻¹ * Real.pi * Complex.I * h.val * y.val * 2) * Complex.exp (-(q n : ℂ)⁻¹ * Real.pi * Complex.I * h.val * x.val * 2) := by
          intro x y; rw [ ← Complex.exp_add ] ; ring_nf;
          rw [ Complex.exp_eq_exp_iff_exists_int ];
          -- Since $y$ and $x$ are elements of $ZMod (q n)$, their values are integers modulo $q n$. Therefore, $(y - x).val$ is equal to $y.val - x.val$ plus some multiple of $q n$.
          obtain ⟨k, hk⟩ : ∃ k : ℤ, (y - x).val = y.val - x.val + k * q n := by
            have h_mod : (y - x).val ≡ y.val - x.val [ZMOD q n] := by
              simp +decide [ ← ZMod.intCast_eq_intCast_iff ];
            exact h_mod.symm.dvd.imp fun k hk => by linarith;
          use k * h.val; push_cast [ ← @Int.cast_inj ℂ ] at *; rw [ hk ] ; ring_nf;
          rw [ mul_inv_cancel₀ ( Nat.cast_ne_zero.mpr <| ne_of_gt <| Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2 ) ] ; ring;
        simp +decide only [mul_assoc, Finset.mul_sum _ _ _, Finset.sum_mul];
        exact Finset.sum_comm.trans ( Finset.sum_congr rfl fun _ _ => Finset.sum_congr rfl fun _ _ => by push_cast [ ← mul_assoc, ← Complex.exp_add ] ; rw [ h_exp ] ; ring_nf )

/-
Standard Parseval's Identity: The sum of the squared magnitudes of the Fourier coefficients equals the average of the squared function values.
-/
lemma parseval_standard (n : ℕ) (r : Fin (w n) → ℕ) :
    ∑ h : ZMod (q n), Complex.normSq (f_hat n r h) = (1 / (q n : ℝ)) * ∑ x : ZMod (q n), (f n r x)^2 := by
      -- Applying the expansion for each term in the sum:
      have h_sum : ∑ h : ZMod (q n), Complex.normSq (f_hat n r h) = (1 / (q n : ℂ)^2) * ∑ x : ZMod (q n), ∑ y : ZMod (q n), (f n r x : ℂ) * (f n r y : ℂ) * ∑ h : ZMod (q n), Complex.exp (2 * Real.pi * Complex.I * h.val * (y - x).val / (q n : ℂ)) := by
        -- Apply the norm squared expansion to each term in the sum.
        have h_expand : ∀ h : ZMod (q n), Complex.normSq (f_hat n r h) = (1 / (q n : ℂ)^2) * ∑ x : ZMod (q n), ∑ y : ZMod (q n), (f n r x : ℂ) * (f n r y : ℂ) * Complex.exp (2 * Real.pi * Complex.I * h.val * (y - x).val / (q n : ℂ)) := by
          exact fun h ↦ f_hat_normSq_expansion n r h;
        simp +zetaDelta at *;
        simp +decide only [h_expand, Finset.mul_sum] ; ring_nf;
        exact Finset.sum_comm.trans ( Finset.sum_congr rfl fun _ _ => Finset.sum_comm.trans ( Finset.sum_congr rfl fun _ _ => Finset.sum_congr rfl fun _ _ => by ring ) );
      -- Substitute the result from h_double_sum into h_sum.
      have h_final : ∑ h : ZMod (q n), Complex.normSq (f_hat n r h) = (1 / (q n : ℂ)^2) * (q n : ℂ) * ∑ x : ZMod (q n), (f n r x : ℂ)^2 := by
        rw [ h_sum, sum_orthogonality_application ] ; ring;
      convert h_final using 1 ; norm_num [ sq, mul_assoc ] ; ring_nf;
      norm_num [ ← Complex.ofReal_inj ]

/-
Expansion of the variance formula.
-/
lemma variance_expansion (n : ℕ) (r : Fin (w n) → ℕ) :
    σ_sq n r = (1 / (q n : ℝ)) * ∑ x : ZMod (q n), (f n r x)^2 - (μ n r)^2 := by
      unfold σ_sq μ;
      rw [ show f n r = fun x => if x ∈ A n r then 1 else 0 from funext fun x => rfl ] ; norm_num [ Finset.sum_ite, Finset.filter_mem_eq_inter, Finset.filter_not ] ; ring_nf;
      norm_num [ Finset.sum_add_distrib, Finset.mul_sum _ _ _, Finset.sum_mul ] ; ring_nf!;
      by_cases h : q n = 0 <;> simp_all +decide [ pow_three, sq, mul_assoc ] ; ring

/-
Lemma 1 (Parseval's Identity for the Sieve): The variance of the indicator function f is strictly equal to the sum of the squared magnitudes of its non-zero Fourier coefficients.
-/
theorem parseval_identity (n : ℕ) (r : Fin (w n) → ℕ) :
    σ_sq n r = ∑ h : ZMod (q n), if h = 0 then 0 else Complex.normSq (f_hat n r h) := by
      rw [ Finset.sum_ite, Finset.filter_ne' ];
      have := parseval_standard n r; have := variance_expansion n r; simp_all +decide [ sub_eq_add_neg ] ;
      rw [ f_hat_zero_eq_mu ] ; norm_num [ Complex.normSq ] ; ring;

/-
The primorial q is odd.
-/
lemma q_odd (n : ℕ) : Odd (q n) := by
  -- Each prime $p$ in $\mathcal{P}_n$ is odd, and the product of odd numbers is odd.
  have h_odd_primes : ∀ p ∈ P_n n, Odd p := by
    exact fun p hp => Nat.Prime.odd_of_ne_two ( Finset.mem_filter.mp hp |>.2.2 ) ( by linarith [ Finset.mem_filter.mp hp |>.2.1 ] );
  exact Nat.odd_iff.mpr ( Finset.prod_nat_mod _ _ _ ▸ by rw [ Finset.prod_eq_one fun x hx => Nat.odd_iff.mp ( h_odd_primes x hx ) ] ; norm_num )

/-
Conjugate symmetry of the Fourier transform for real-valued functions.
-/
lemma f_hat_conj_symm (n : ℕ) (r : Fin (w n) → ℕ) (h : ZMod (q n)) :
    f_hat n r (-h) = starRingEnd ℂ (f_hat n r h) := by
      unfold f_hat;
      -- Since $(-h).val \equiv -h.val \pmod{q}$, we can replace $(-h).val$ with $-h.val$ in the exponent.
      have h_exp : ∀ x : ZMod (q n), Complex.exp (-2 * Real.pi * Complex.I * ((-h).val * x.val) / (q n : ℂ)) = Complex.exp (2 * Real.pi * Complex.I * (h.val * x.val) / (q n : ℂ)) := by
        have h_exp : (-h).val ≡ -h.val [ZMOD q n] := by
          simp +decide [ ← ZMod.intCast_eq_intCast_iff ];
        obtain ⟨ k, hk ⟩ := h_exp.symm.dvd;
        intro x; rw [ Complex.exp_eq_exp_iff_exists_int ] ; use -k * x.val; push_cast [ ← @Int.cast_inj ℂ ] at *; simp_all +decide [ mul_assoc, mul_comm, mul_left_comm, div_eq_mul_inv ] ; ring_nf;
        field_simp;
        rw [ div_sub', mul_div_assoc' ];
        · grind;
        · exact Nat.cast_ne_zero.mpr <| Nat.ne_of_gt <| Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2;
      simp +zetaDelta at *;
      left; congr; ext x; rw [ h_exp ] ; rw [ Complex.ext_iff ] ; norm_num [ Complex.exp_re, Complex.exp_im, neg_div ] ; ring_nf;
      erw [ ZMod.cast_eq_val, ZMod.cast_eq_val ] ; norm_cast ; aesop

/-
Lemma 2 (Harmonic Variance Lower Bound): For any non-zero frequency h, the variance is at least twice the squared magnitude of the Fourier coefficient at h.
-/
lemma harmonic_variance_lower_bound (n : ℕ) (r : Fin (w n) → ℕ) (h : ZMod (q n)) (h_ne_zero : h ≠ 0) :
    σ_sq n r ≥ 2 * Complex.normSq (f_hat n r h) := by
      have := f_hat_conj_symm n r h;
      rw [ parseval_identity ];
      rw [ Finset.sum_eq_add_sum_diff_singleton ( Finset.mem_univ h ) ];
      rw [ Finset.sum_eq_add_sum_diff_singleton ( Finset.mem_sdiff.mpr ⟨ Finset.mem_univ ( -h ), ?_ ⟩ ) ] <;> norm_num [ h_ne_zero, this ];
      · linarith [ show 0 ≤ ∑ x ∈ ( Finset.univ \ { h } ) \ { -h }, if x = 0 then 0 else Complex.normSq ( f_hat n r x ) from Finset.sum_nonneg fun x hx => by split_ifs <;> norm_num [ Complex.normSq_nonneg ] ];
      · intro h_eq; have := q_odd n; simp_all +decide [ neg_eq_iff_add_eq_zero ] ;

/-
Define the Krafft tuple r^K such that for each 1 <= i <= w, r^K_i = floor((p_i+1)/6).
-/
def r_K (n : ℕ) (i : Fin (w n)) : ℕ := (p n i + 1) / 6

/-
Define the target interval of indices: A_n = {x in N | 6n^2 - 2n <= x <= 6n^2 + 10n + 3}.
-/
def A_n (n : ℕ) : Finset ℕ := Finset.Icc (6 * n^2 - 2 * n) (6 * n^2 + 10 * n + 3)

/-
As defined previously, an integer x survives the sieve (i.e., f(x) = 1) if and only if x is not congruent to +/ - r^K_i mod p_i for all 1 <= i <= w.
-/
theorem survives_iff (n : ℕ) (x : ZMod (q n)) :
    f n (r_K n) x = 1 ↔ ∀ i : Fin (w n), (x.cast : ZMod (p n i)) ≠ r_K n i ∧ (x.cast : ZMod (p n i)) ≠ -r_K n i := by
      unfold f A A_i; simp +decide [ Finset.mem_filter ] ;

/-
Lemma 3.1 (Krafft Algebraic Equivalence): For any prime p >= 5 and any integer x, let r = floor((p+1)/6). Then x = +/ - r (mod p) if and only if p | (6x-1) or p | (6x+1).
-/
lemma krafft_algebraic_equivalence (p : ℕ) (hp : p.Prime) (h_ge_5 : p ≥ 5) (x : ℤ) :
    let r := (p + 1) / 6
    (x : ZMod p) = r ∨ (x : ZMod p) = -r ↔ (p : ℤ) ∣ (6 * x - 1) ∨ (p : ℤ) ∣ (6 * x + 1) := by
      simp_all +decide [ ← ZMod.intCast_zmod_eq_zero_iff_dvd ];
      haveI := Fact.mk hp; norm_num [ eq_comm, sub_eq_zero, add_eq_zero_iff_eq_neg ] ;
      have h_mul : (6 * ((p + 1) / 6 : ℕ) : ZMod p) = 1 ∨ (6 * ((p + 1) / 6 : ℕ) : ZMod p) = -1 := by
        have h_mul : (6 * ((p + 1) / 6 : ℕ) : ℕ) ≡ 1 [ZMOD p] ∨ (6 * ((p + 1) / 6 : ℕ) : ℕ) ≡ -1 [ZMOD p] := by
          have h_mod : p % 6 = 1 ∨ p % 6 = 5 := by
            by_contra h_contra; have := Nat.Prime.eq_two_or_odd hp; ( have := Nat.dvd_of_mod_eq_zero ( show p % 3 = 0 from by omega ) ; rw [ hp.dvd_iff_eq ] at this <;> linarith; );
          cases h_mod <;> simp_all +decide [ Int.ModEq ];
          · exact Or.inr ( Int.modEq_iff_dvd.mpr ⟨ -1, by linarith [ Nat.mod_add_div ( p + 1 ) 6, show ( p + 1 ) % 6 = 2 from by norm_num [ *, Nat.add_mod ] ] ⟩ );
          · exact Or.inl <| Int.modEq_iff_dvd.mpr ⟨ -1, by linarith [ Nat.mod_add_div ( p + 1 ) 6, show ( p + 1 ) % 6 = 0 from by norm_num [ *, Nat.add_mod ] ] ⟩;
        simp_all +decide [ ← ZMod.intCast_eq_intCast_iff ];
        norm_cast at *;
        aesop;
      grind

/-
Lemma 3.2 (Interval Projection Bound): For any x in A_n, the value 6x+1 is strictly bounded by the square of the next possible prime after P_n: 6x + 1 < (6n+5)^2.
-/
lemma interval_projection_bound (n : ℕ) (x : ℕ) (hx : x ∈ A_n n) :
    6 * x + 1 < (6 * n + 5) ^ 2 := by
      unfold A_n at hx; rcases n with ( _ | _ | n ) <;> norm_num at * ; linarith [ Nat.sub_add_cancel ( by nlinarith : 2 * ( ‹_› : ℕ ) ≤ 6 * ‹_› ^ 2 ) ] ;
      · grind;
      · linarith [ sq n ]

/-
Any prime p such that 5 <= p < 6n+5 must be in P_n. This is because the integers 6n+2, 6n+3, 6n+4 are all composite (divisible by 2 or 3), so there are no primes in the interval [6n+2, 6n+4].
-/
lemma primes_in_range_eq_P_n (n : ℕ) (p : ℕ) (hp : p.Prime) (h_ge_5 : 5 ≤ p) (h_lt : p < 6 * n + 5) :
    p ∈ P_n n := by
      by_cases h_cases : p = 6 * n + 4 ∨ p = 6 * n + 3 ∨ p = 6 * n + 2;
      · rcases h_cases with ( rfl | rfl | rfl ) <;> simp_all +arith;
        · exact absurd hp ( by rw [ show 6 * n + 4 = 2 * ( 3 * n + 2 ) by ring ] ; exact Nat.not_prime_mul ( by norm_num ) ( by linarith ) );
        · exact absurd hp ( by rw [ show 6 * n + 3 = 3 * ( 2 * n + 1 ) by ring ] ; exact Nat.not_prime_mul ( by norm_num ) ( by linarith ) );
        · exact absurd hp ( by rw [ show 6 * n + 2 = 2 * ( 3 * n + 1 ) by ring ] ; exact Nat.not_prime_mul ( by norm_num ) ( by linarith ) );
      · exact Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( by omega ), h_ge_5, hp ⟩

/-
If k >= 2 and k < m^2, and k has no prime factors less than m, then k is prime.
-/
lemma prime_of_no_prime_factors_lt {k m : ℕ} (hk : 2 ≤ k) (h_sq : k < m ^ 2) (h_no_factors : ∀ p, p.Prime → p < m → ¬ p ∣ k) : k.Prime := by
  by_contra h_not_prime;
  -- Since $k$ is not prime, it must have a prime factor $p$ such that $p \leq \sqrt{k}$.
  obtain ⟨p, hp_prime, hp_div⟩ : ∃ p, Nat.Prime p ∧ p ∣ k ∧ p ≤ Nat.sqrt k := by
    obtain ⟨ p, hp₁, hp₂ ⟩ := Nat.exists_prime_and_dvd ( by linarith );
    obtain ⟨ q, rfl ⟩ := hp₂;
    exact ⟨ Nat.minFac ( p * q ), Nat.minFac_prime ( by linarith ), Nat.minFac_dvd _, by rw [ Nat.le_sqrt ] ; nlinarith [ Nat.minFac_le_of_dvd ( Nat.one_lt_iff_ne_zero_and_ne_one.mpr ⟨ by aesop_cat, by aesop_cat ⟩ ) ( dvd_mul_right p q ), Nat.minFac_le_of_dvd ( Nat.one_lt_iff_ne_zero_and_ne_one.mpr ⟨ by aesop_cat, by aesop_cat ⟩ ) ( dvd_mul_left q p ) ] ⟩;
  exact h_no_factors p hp_prime ( by nlinarith [ Nat.sqrt_le k ] ) hp_div.1

/-
A number p is in P_n if and only if it equals p_i for some index i.
-/
lemma mem_P_n_iff_exists_index (n : ℕ) (p_val : ℕ) :
    p_val ∈ P_n n ↔ ∃ i : Fin (w n), p n i = p_val := by
      constructor <;> intro h;
      · -- By definition of $P_n$, since $p_val \in P_n$, it must be one of the primes in the sorted list.
        have h_in_sorted : p_val ∈ primes_list n := by
          exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.2 h;
        obtain ⟨ i, hi ⟩ := List.mem_iff_get.1 h_in_sorted; use ⟨ i, by
          exact i.2.trans_le ( by simp +decide [ primes_list, w ] ) ⟩ ; aesop;
      · obtain ⟨ i, rfl ⟩ := h;
        exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ )

/-
If x survives, then for any p in P_n, p does not divide 6x-1 and p does not divide 6x+1.
-/
lemma not_dvd_of_survives (n : ℕ) (x : ℕ) (h_survives : f n (r_K n) x = 1) (p : ℕ) (hp : p ∈ P_n n) :
    ¬(p : ℤ) ∣ (6 * x - 1) ∧ ¬(p : ℤ) ∣ (6 * x + 1) := by
      obtain ⟨ i, hi ⟩ := mem_P_n_iff_exists_index n p |>.1 hp;
      have h_cong : (x.cast : ZMod p) ≠ r_K n i ∧ (x.cast : ZMod p) ≠ -r_K n i := by
        convert survives_iff n x |>.1 h_survives i ; aesop;
        all_goals subst hi; norm_cast;
        · erw [ ZMod.cast_eq_val ];
          norm_num [ ZMod.natCast_zmod_val ];
          rw [ ← ZMod.natCast_mod ];
          rw [ ← Nat.mod_mod_of_dvd x ( show p n i ∣ q n from Finset.dvd_prod_of_mem _ hp ) ];
          exact ZMod.natCast_mod (x % q n) (p n i);
        · erw [ ZMod.cast_eq_val ];
          norm_num [ ZMod.natCast_zmod_val ];
          rw [ ZMod.natCast_eq_natCast_iff ];
          rw [ Nat.ModEq, Nat.mod_mod_of_dvd _ ( show p n i ∣ q n from Finset.dvd_prod_of_mem _ hp ) ];
      have := krafft_algebraic_equivalence p ( Finset.mem_filter.mp hp |>.2.2 ) ( Finset.mem_filter.mp hp |>.2.1 ) x; simp_all +decide [ ← ZMod.intCast_zmod_eq_zero_iff_dvd ] ;
      simp_all +decide [ r_K ]

/-
6x-1 and 6x+1 are not divisible by 2 or 3.
-/
lemma not_dvd_two_three (x : ℤ) :
    ¬(2 : ℤ) ∣ (6 * x - 1) ∧ ¬(3 : ℤ) ∣ (6 * x - 1) ∧
    ¬(2 : ℤ) ∣ (6 * x + 1) ∧ ¬(3 : ℤ) ∣ (6 * x + 1) := by
      grind +ring

/-
Lemma 3.3 (The Sieve Isomorphism): Assume x in A_n. By the Fundamental Theorem of Arithmetic and the Sieve of Eratosthenes bound from Lemma 3.2, x survives the Krafft sieve if and only if both 6x-1 and 6x+1 are prime numbers.
-/
lemma sieve_isomorphism (n : ℕ) (hn : n ≥ 1) (x : ℕ) (hx : x ∈ A_n n) :
    f n (r_K n) x = 1 ↔ Nat.Prime (6 * x - 1) ∧ Nat.Prime (6 * x + 1) := by
      -- Apply the lemma survives_iff to rewrite the goal in terms of the congruence conditions.
      rw [survives_iff];
      constructor <;> intro h;
      · -- Since 6x-1 and 6x+1 have no prime factors less than 6n+5, they must be prime.
        have h_prime_factors : ∀ p : ℕ, p.Prime → p < 6 * n + 5 → ¬(p : ℤ) ∣ (6 * x - 1) ∧ ¬(p : ℤ) ∣ (6 * x + 1) := by
          intro p hp hp_lt
          by_cases hp_ge_5 : 5 ≤ p;
          · apply not_dvd_of_survives n x (by
            exact (survives_iff n ↑x).mpr h) p (by
            exact primes_in_range_eq_P_n n p hp hp_ge_5 hp_lt);
          · interval_cases p <;> norm_num at * <;> omega;
        apply And.intro;
        · apply prime_of_no_prime_factors_lt;
          any_goals exact 6 * n + 5;
          · exact le_tsub_of_add_le_left ( by linarith [ show x ≥ 1 from Nat.pos_of_ne_zero ( by rintro rfl; exact absurd hx ( by unfold A_n; norm_num; nlinarith [ Nat.sub_add_cancel ( by nlinarith : 2 * n ≤ 6 * n ^ 2 ) ] ) ) ] );
          · rw [ tsub_lt_iff_left ] <;> nlinarith only [ hn, Finset.mem_Icc.mp hx, Nat.sub_add_cancel ( by nlinarith only [ hn, Finset.mem_Icc.mp hx ] : 2 * n ≤ 6 * n ^ 2 ) ];
          · intro p pp p5; specialize h_prime_factors p pp p5; rcases x with ( _ | x ) <;> simp_all +decide [ ← Int.natCast_dvd_natCast ] ;
            exact absurd hx ( by rw [ A_n ] ; norm_num; nlinarith [ Nat.sub_add_cancel ( by nlinarith : 2 * n ≤ 6 * n ^ 2 ) ] );
        · apply prime_of_no_prime_factors_lt;
          any_goals exact 6 * n + 5;
          · linarith [ show x > 0 from Nat.pos_of_ne_zero ( by rintro rfl; exact absurd hx ( by unfold A_n; norm_num; nlinarith [ Nat.sub_add_cancel ( show 2 * n ≤ 6 * n ^ 2 by nlinarith ) ] ) ) ];
          · exact interval_projection_bound n x hx;
          · exact fun p pp p5 => mod_cast h_prime_factors p pp p5 |>.2;
      · intro i
        have h_not_div : ¬(p n i : ℤ) ∣ (6 * x - 1) ∧ ¬(p n i : ℤ) ∣ (6 * x + 1) := by
          constructor <;> intro H <;> norm_cast at *;
          · rw [ Int.subNatNat_of_le ( by linarith [ show x > 0 from Nat.pos_of_ne_zero ( by rintro rfl; contradiction ) ] ) ] at H ; norm_cast at H ; simp_all;
            rw [ Nat.dvd_prime h.1 ] at H;
            have h_pi_lt : p n i < 6 * n + 2 := by
              have h_pi_lt : p n i ∈ P_n n := by
                exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
              exact Finset.mem_range.mp ( Finset.mem_filter.mp h_pi_lt |>.1 );
            rcases x with ( _ | _ | x ) <;> simp_all +arith +decide [ Nat.mul_succ ];
            · unfold A_n at hx; norm_num at hx; nlinarith;
            · cases H <;> simp_all +arith +decide [ A_n ];
              · have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop;
              · nlinarith only [ hx, h_pi_lt ];
          · rw [ Nat.dvd_prime h.2 ] at H;
            -- Since $p n i$ is a prime number in $P_n$, it must be at least 5. However, $6x + 1$ is greater than $6n + 2$, which is the upper bound for $P_n$. Therefore, $p n i$ cannot be $6x + 1$.
            have h_contra : p n i ≤ 6 * n + 1 := by
              have h_contra : p n i ∈ P_n n := by
                exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
              exact Nat.le_of_lt_succ ( Finset.mem_range.mp ( Finset.mem_filter.mp h_contra |>.1 ) );
            cases H <;> simp_all +arith +decide [ A_n ];
            · have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop;
            · nlinarith only [ hn, hx, h_contra ];
        have := krafft_algebraic_equivalence ( p n i ) ( Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.2.2 ) ( by
                                                                                  have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop; ) x
        generalize_proofs at *;
        simp_all +decide [ r_K ];
        convert this using 1;
        · norm_num [ ZMod.natCast_eq_natCast_iff' ];
          erw [ ZMod.cast_eq_val ];
          erw [ ZMod.val_natCast ];
          erw [ ZMod.natCast_eq_natCast_iff ];
          rw [ Nat.ModEq, Nat.mod_mod_of_dvd _ ( show p n i ∣ q n from Finset.dvd_prod_of_mem _ <| by
                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ];
        · norm_num [ ZMod.cast, ZMod.val ];
          cases h : q n <;> simp_all +decide [ ZMod ];
          convert this.2 using 1;
          rw [ ← ZMod.natCast_mod ];
          rw [ Nat.mod_mod_of_dvd _ ( show p n i ∣ _ from _ ) ];
          · simp;
          · exact h ▸ Finset.dvd_prod_of_mem _ ( Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) )


/-
Define the interval indicator function I_A which is 1 if the representative of x is in A_n, and 0 otherwise.
-/
/-- Define the interval indicator function $I_{\mathcal{A}} : \mathbb{Z}/q\mathbb{Z} \to \mathbb{R}$ such that $I_{\mathcal{A}}(x) = 1$ if $x \in \mathcal{A}_n$, and $0$ otherwise. -/
noncomputable def I_A (n : ℕ) (x : ZMod (q n)) : ℝ :=
  if x.val ∈ A_n n then 1 else 0

/-
Define L as the cardinality of A_n and prove it equals 12n + 4.
-/
/-- Define the length of the interval $L = |\mathcal{A}_n|$. -/
def L (n : ℕ) : ℕ := (A_n n).card

/-- The length of the interval is $12n + 4$. -/
theorem L_eq (n : ℕ) : L n = 12 * n + 4 := by
  rw [ show L n = Finset.card ( Finset.Icc ( 6 * n ^ 2 - 2 * n ) ( 6 * n ^ 2 + 10 * n + 3 ) ) by rfl, Nat.card_Icc ] ; ring_nf;
  exact Nat.sub_eq_of_eq_add <| by linarith [ Nat.sub_add_cancel <| show n * 2 ≤ n ^ 2 * 6 from by nlinarith [ show n ≤ n ^ 2 by nlinarith ] ] ;

/-
Define the Discrete Fourier Transform of I_A.
-/
/-- Define the Discrete Fourier Transform of $I_{\mathcal{A}}$ at frequency $h$:
    $$ \hat{I}_{\mathcal{A}}(h) = \frac{1}{q} \sum_{x=0}^{q-1} I_{\mathcal{A}}(x) e^{-2\pi i h x / q} $$ -/
noncomputable def I_A_hat (n : ℕ) (h : ZMod (q n)) : ℂ :=
  haveI : NeZero (q n) := ⟨by
    exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2⟩
  (1 / (q n : ℂ)) * ∑ x : ZMod (q n), (I_A n x : ℂ) * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ))

/-
Define S as the sum of f(x) * I_A(x) over ZMod q.
-/
/-- Let $S$ be the total number of survivors in $\mathcal{A}_n$:
    $$ S = \sum_{x \in \mathbb{Z}/q\mathbb{Z}} f(x) I_{\mathcal{A}}(x) $$ -/
noncomputable def S (n : ℕ) (r : Fin (w n) → ℕ) : ℝ :=
  ∑ x : ZMod (q n), f n r x * I_A n x

/-
P_n is a subset of P_{n+1}.
-/
lemma P_n_mono (n : ℕ) : P_n n ⊆ P_n (n + 1) := by
  exact fun x hx => Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( by linarith [ Finset.mem_range.mp ( Finset.mem_filter.mp hx |>.1 ), Finset.mem_filter.mp hx |>.2.2.two_le ] ), Finset.mem_filter.mp hx |>.2.1, Finset.mem_filter.mp hx |>.2.2 ⟩ ;

/-
q(n) divides q(n+1).
-/
lemma q_mono (n : ℕ) : q n ∣ q (n + 1) := by
  -- Since $P_n \subseteq P_{n+1}$, the product of elements in $P_n$ divides the product of elements in $P_{n+1}$.
  apply Finset.prod_dvd_prod_of_subset; exact P_n_mono n

/-
q(n) is at least 35 for n >= 1.
-/
lemma q_ge_35 (n : ℕ) (hn : n ≥ 1) : q n ≥ 35 := by
  -- Since $q(n)$ is monotonic (in the sense of divisibility), we have $q(1) \mid q(n)$.
  have h_div : q 1 ∣ q n := by
    induction hn <;> [ tauto; exact dvd_trans ‹_› ( q_mono _ ) ];
  exact Nat.le_of_dvd ( Nat.pos_of_ne_zero ( by unfold q; exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2 ) ) h_div |> le_trans ( by native_decide )

/-
q(n) is at least 5005 for n >= 2.
-/
lemma q_ge_5005 (n : ℕ) (hn : n ≥ 2) : q n ≥ 5005 := by
  -- Since $q(n)$ is monotonic (divisibility), for $n \ge 2$, $q(n)$ is a multiple of $q(2) = 5005$.
  have h_div : q 2 ∣ q n := by
    induction hn <;> [ tauto; exact dvd_trans ‹_› ( q_mono _ ) ];
  exact Nat.le_of_dvd ( Nat.pos_of_ne_zero ( by unfold q; exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2 ) ) h_div |> le_trans ( by native_decide )

/-
q(3) is 1616615.
-/
lemma q_3_val : q 3 = 1616615 := by
  native_decide +revert

/-
q(n) is at least 1616615 for n >= 3.
-/
lemma q_ge_q3 (n : ℕ) (hn : n ≥ 3) : q n ≥ 1616615 := by
  induction' n, hn using Nat.le_induction with n hn ih;
  · native_decide +revert;
  · exact le_trans ih ( Nat.le_of_dvd ( Finset.prod_pos fun p hp => Nat.Prime.pos ( Finset.mem_filter.mp hp |>.2.2 ) ) ( q_mono n ) )

/-
q(4) is 37182145.
-/
lemma q_4_val : q 4 = 37182145 := by
  native_decide +revert

/-
q(n) is at least 37182145 for n >= 4.
-/
lemma q_ge_q4 (n : ℕ) (hn : n ≥ 4) : q n ≥ 37182145 := by
  induction' hn with n hn ih;
  · native_decide +revert;
  · exact le_trans ih ( Nat.le_of_dvd ( Finset.prod_pos fun p hp => Nat.Prime.pos ( Finset.mem_filter.mp hp |>.2.2 ) ) ( q_mono n ) )

/-
q(10) is greater than 10^10.
-/
lemma q_10_large : q 10 > 10^10 := by
  native_decide +revert

/-
q(n) is at least 10^10 for n >= 10.
-/
lemma q_ge_q10 (n : ℕ) (hn : n ≥ 10) : q n ≥ 10^10 := by
  induction' hn with n hn ih <;> norm_num [ * ] at *;
  · native_decide +revert;
  · exact le_trans ih ( Nat.le_of_dvd ( Finset.prod_pos fun p hp => Nat.Prime.pos ( Finset.mem_filter.mp hp |>.2.2 ) ) ( q_mono n ) )

/-
q(10) is greater than 10^20.
-/
lemma q_10_very_large : q 10 > 10^20 := by
  native_decide +revert

/-
q(n) is at least 10^20 for n >= 10.
-/
lemma q_ge_q10_very_large (n : ℕ) (hn : n ≥ 10) : q n ≥ 10^20 := by
  induction' n, hn using Nat.le_induction with n hn ih;
  · native_decide +revert;
  · refine' le_trans ih _;
    exact Nat.le_of_dvd ( by exact Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2 ) ( q_mono n )

/-
For n >= 1, 6n^2 + 10n + 3 < q(n).
-/
theorem q_bound (n : ℕ) (hn : n ≥ 1) : 6 * n^2 + 10 * n + 3 < q n := by
  by_cases hn10 : n < 10
  · interval_cases n <;> native_decide
  · have hn_ge_10 : n ≥ 10 := by linarith
    have h_q_ge : q n ≥ 10^20 := q_ge_q10_very_large n hn_ge_10
    by_cases hn_small : n ≤ 1000000000
    · apply lt_of_lt_of_le _ h_q_ge
      calc 6 * n^2 + 10 * n + 3
        _ ≤ 6 * 1000000000^2 + 10 * 1000000000 + 3 := by nlinarith
        _ < 10^20 := by native_decide
    · have hn_large : n > 1000000000 := by linarith
      -- For n > 10^9, q(n) is extremely large.
      -- We assume this holds.
      -- Since $q(n)$ is the product of primes in $P_n$, and for $n > 1,000,000,000$, there are many primes in $P_n$, $q(n)$ is extremely large.
      have h_q_large : q n ≥ 5 ^ (Nat.log 2 n) := by
        have h_q_large : q n ≥ 5 ^ (Finset.card (Finset.filter (fun p => 5 ≤ p ∧ p.Prime) (Finset.range (6 * n + 2)))) := by
          exact le_trans ( by norm_num ) ( Finset.prod_le_prod' fun x hx => show x ≥ 5 from Finset.mem_filter.mp hx |>.2.1 ) |> le_trans <| Finset.prod_le_prod_of_subset_of_one_le' ( Finset.filter_subset_filter _ <| Finset.range_mono <| Nat.le_refl _ ) fun x hx _ => Nat.one_le_iff_ne_zero.mpr <| Nat.Prime.ne_zero <| Finset.mem_filter.mp hx |>.2.2;
        refine le_trans ?_ h_q_large;
        refine' pow_le_pow_right₀ ( by norm_num ) _;
        refine' le_trans _ ( Finset.card_mono <| show Finset.filter ( fun p => 5 ≤ p ∧ Nat.Prime p ) ( Finset.range ( 6 * n + 2 ) ) ≥ Finset.image ( fun k => Nat.nth Nat.Prime k ) ( Finset.Ico 2 ( Nat.log 2 n + 2 ) ) from _ );
        · rw [ Finset.card_image_of_injective _ fun a b h => Nat.nth_injective ( Nat.infinite_setOf_prime ) h ] ; simp +arith +decide;
        · -- To prove the inequality, it suffices to show that for any $k$ in the range $2$ to $\log_2(n) + 1$, the $k$-th prime is less than $6n + 2$.
          have h_prime_bound : ∀ k ∈ Finset.Ico 2 (Nat.log 2 n + 2), Nat.nth Nat.Prime k < 6 * n + 2 := by
            intros k hk
            have h_prime_bound : Nat.nth Nat.Prime k ≤ 2 ^ (k + 1) := by
              refine' Nat.le_of_lt_succ _;
              refine' Nat.nth_lt_of_lt_count _;
              refine' Nat.le_induction _ _ k ( show k ≥ 2 from Finset.mem_Ico.mp hk |>.1 ) <;> intros <;> simp_all +decide [ Nat.pow_succ' ];
              rw [ Nat.count_eq_card_filter_range ] at *;
              refine' lt_of_le_of_lt ( Nat.succ_le_of_lt ‹_› ) _;
              refine' Finset.card_lt_card _;
              norm_num [ Finset.ssubset_def, Finset.subset_iff ];
              exact ⟨ fun x hx₁ hx₂ => ⟨ by linarith, hx₂ ⟩, by rcases Nat.exists_prime_lt_and_le_two_mul ( 2 * 2 ^ ‹_› ) ( by linarith [ Nat.one_le_pow ‹_› 2 zero_lt_two ] ) with ⟨ p, hp₁, hp₂ ⟩ ; exact ⟨ p, by linarith, hp₁, fun hx₃ => by linarith ⟩ ⟩;
            refine lt_of_le_of_lt h_prime_bound ?_;
            exact lt_of_le_of_lt ( pow_le_pow_right₀ ( by decide ) ( show k + 1 ≤ Nat.log 2 n + 2 by linarith [ Finset.mem_Ico.mp hk ] ) ) ( by rw [ pow_add ] ; nlinarith [ Nat.pow_log_le_self 2 ( by linarith : n ≠ 0 ) ] );
          simp +zetaDelta at *;
          exact Finset.image_subset_iff.mpr fun k hk => Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( h_prime_bound k ( Finset.mem_Ico.mp hk |>.1 ) ( Finset.mem_Ico.mp hk |>.2 ) ), by linarith [ Nat.Prime.two_le ( Nat.prime_nth_prime k ), show Nat.nth Nat.Prime k ≥ 5 from Nat.le_trans ( by norm_num ) ( Nat.nth_monotone ( Nat.infinite_setOf_prime ) ( show k ≥ 2 from Finset.mem_Ico.mp hk |>.1 ) ) ], Nat.prime_nth_prime k ⟩;
      refine lt_of_lt_of_le ?_ h_q_large;
      -- We'll use that $5^{Nat.log 2 n}$ grows much faster than $6n^2 + 10n + 3$.
      have h_exp_growth : ∀ n ≥ 1000000000, 5 ^ (Nat.log 2 n) > 6 * n ^ 2 + 10 * n + 3 := by
        intro n hn
        induction' n using Nat.strong_induction_on with n ih;
        by_cases hn_large : n ≥ 2 ^ 30;
        · have h_exp_growth : 5 ^ (Nat.log 2 n) ≥ 5 ^ (Nat.log 2 (n / 2) + 1) := by
            rw [ show Nat.log 2 n = Nat.log 2 ( n / 2 ) + 1 from ?_ ];
            rw [ Nat.log_eq_iff ] <;> norm_num;
            rw [ Nat.sub_add_cancel ( Nat.log_pos ( by norm_num ) ( by linarith ) ) ] ; exact ⟨ Nat.pow_le_of_le_log ( by linarith ) ( by linarith ), Nat.lt_pow_of_log_lt ( by linarith ) ( by linarith ) ⟩ ;
          have h_exp_growth : 5 ^ (Nat.log 2 (n / 2) + 1) ≥ 5 * (6 * (n / 2) ^ 2 + 10 * (n / 2) + 3) := by
            have h_exp_growth : 5 ^ (Nat.log 2 (n / 2)) > 6 * (n / 2) ^ 2 + 10 * (n / 2) + 3 := by
              by_cases hn_large : n / 2 ≥ 1000000000;
              · exact ih _ ( Nat.div_lt_self ( by linarith ) ( by norm_num ) ) hn_large;
              · have h_exp_growth : Nat.log 2 (n / 2) ≥ 29 := by
                  exact Nat.le_log_of_pow_le ( by norm_num ) ( by omega );
                exact lt_of_lt_of_le ( by nlinarith only [ hn_large, Nat.div_add_mod n 2, Nat.mod_lt n two_pos ] ) ( Nat.pow_le_pow_right ( by decide ) h_exp_growth );
            rw [ pow_succ' ] ; linarith;
          nlinarith only [ hn_large, Nat.div_add_mod n 2, Nat.mod_lt n two_pos, h_exp_growth, ‹5 ^ Nat.log 2 n ≥ 5 ^ ( Nat.log 2 ( n / 2 ) + 1 ) › ];
        · have : Nat.log 2 n ≥ 29 := Nat.le_log_of_pow_le ( by decide ) ( by linarith ) ; ( have : Nat.log 2 n ≤ 29 := Nat.le_of_lt_succ ( Nat.log_lt_of_lt_pow ( by linarith ) ( by linarith ) ) ; interval_cases Nat.log 2 n ; norm_num at *; );
          nlinarith only [ hn, hn_large ];
      exact h_exp_growth n hn_large.le

/-
Define the local hit function g_i(x) which is 1 if x is congruent to +/ - r^K_i mod p_i, and 0 otherwise.
-/
/-- Define the local hit function $g_i : \mathbb{Z}/q\mathbb{Z} \to \mathbb{R}$ for each prime index $i \in \{1, \dots, w\}$.
$g_i(x) = 1$ if $x \equiv r^K_i \pmod{p_i}$ or $x \equiv -r^K_i \pmod{p_i}$.
Otherwise, $g_i(x) = 0$. -/
noncomputable def g (n : ℕ) (i : Fin (w n)) (x : ZMod (q n)) : ℝ :=
  if (x.cast : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (x.cast : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then 1 else 0

/-
Define the global additive hit counter c(x) as the sum of all local hits g_i(x).
-/
/-- Define the global additive hit counter $c : \mathbb{Z}/q\mathbb{Z} \to \mathbb{R}$ as the sum of all local hits:
$$ c(x) = \sum_{i=1}^w g_i(x) $$ -/
noncomputable def c (n : ℕ) (x : ZMod (q n)) : ℝ :=
  ∑ i : Fin (w n), g n i x

/-
Lemma 1.3: For x in A_n, c(x) = 0 iff 6x-1 and 6x+1 are prime.
-/
/-- Lemma 1.3 (Additive Sieve Isomorphism):
Prove that an integer $x \in \mathcal{A}_n$ survives the Krafft sieve (meaning both $6x-1$ and $6x+1$ are prime) if and only if its global hit counter is exactly zero:
$$ c(x) = 0 \iff x \text{ survives the Krafft sieve} $$ -/
lemma additive_sieve_isomorphism (n : ℕ) (hn : n ≥ 1) (x : ℕ) (hx : x ∈ A_n n) :
    c n x = 0 ↔ Nat.Prime (6 * x - 1) ∧ Nat.Prime (6 * x + 1) := by
      have := @sieve_isomorphism n hn x ; simp_all ; (
      unfold c f at * ; simp_all ;
      unfold A at this; simp_all ;
      unfold g; simp_all ;
      unfold A_i at this; aesop;)

/-
Define the First Moment M_1(n) as the sum of c(x) for x in A_n.
-/
/-- Define the First Moment $M_1(n)$ as the total number of hits across the target interval $\mathcal{A}_n$:
$$ M_1(n) = \sum_{x \in \mathcal{A}_n} c(x) $$ -/
noncomputable def M_1 (n : ℕ) : ℝ :=
  ∑ x ∈ A_n n, c n (x : ZMod (q n))

/-
Define the Second Moment M_2(n) as the sum of (c(x))^2 for x in A_n.
-/
/-- Define the Second Moment $M_2(n)$ as the sum of the squares of the hits across the target interval $\mathcal{A}_n$:
$$ M_2(n) = \sum_{x \in \mathcal{A}_n} (c(x))^2 $$ -/
noncomputable def M_2 (n : ℕ) : ℝ :=
  ∑ x ∈ A_n n, (c n (x : ZMod (q n)))^2

/-
Lemma 2.1 (First Moment Expansion): By swapping the order of summation, the First Moment M_1(n) is equal to the sum of the local hits over all primes.
-/
theorem first_moment_expansion (n : ℕ) :
    M_1 n = ∑ i : Fin (w n), ∑ x ∈ A_n n, g n i (x : ZMod (q n)) := by
      exact Finset.sum_comm

/-
Lemma 2.2 (Second Moment Expansion): By expanding the square of the sum and swapping the summation order, the Second Moment M_2(n) is equal to the triple sum of g_i(x) g_j(x).
-/
theorem second_moment_expansion (n : ℕ) :
    M_2 n = ∑ i : Fin (w n), ∑ j : Fin (w n), ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n j (x : ZMod (q n)) := by
      -- By Fubini's theorem, we can interchange the order of summation.
      have h_fubini : ∑ x ∈ A_n n, (∑ i : Fin (w n), g n i x)^2 = ∑ i : Fin (w n), ∑ j : Fin (w n), ∑ x ∈ A_n n, g n i x * g n j x := by
        have h_expand : ∀ x ∈ A_n n, (∑ i : Fin (w n), g n i x)^2 = ∑ i : Fin (w n), ∑ j : Fin (w n), g n i x * g n j x := by
          simp +decide [ ← Finset.mul_sum _ _ _, ← Finset.sum_mul, sq ]
        rw [ Finset.sum_congr rfl h_expand, Finset.sum_comm ];
        exact Finset.sum_congr rfl fun _ _ => Finset.sum_comm;
      exact h_fubini

/-
Lemma 2.3 (Local Hit Density Bound): The number of hits in the interval A_n for prime p_i is bounded by 2L/p_i + 2.
-/
theorem local_hit_density_bound (n : ℕ) (i : Fin (w n)) :
    ∑ x ∈ A_n n, g n i (x : ZMod (q n)) ≤ (2 * L n : ℝ) / (p n i : ℝ) + 2 := by
      -- Let's unfold the definition of g to express the sum in terms of modular arithmetic.
      have hg_sum : ∑ x ∈ A_n n, g n i (x : ZMod (q n)) ≤ ∑ x ∈ Finset.range (A_n n).card, (if (6 * n^2 - 2 * n + x : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (6 * n^2 - 2 * n + x : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then 1 else 0) := by
        -- By definition of $g$, we know that $g_i(x) = 1$ if and only if $x \equiv r^K_i \pmod{p_i}$ or $x \equiv -r^K_i \pmod{p_i}$.
        have hg_def : ∀ x ∈ A_n n, g n i (x : ZMod (q n)) = if (6 * n^2 - 2 * n + (x - (6 * n^2 - 2 * n)) : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (6 * n^2 - 2 * n + (x - (6 * n^2 - 2 * n)) : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then 1 else 0 := by
          intros x hx
          simp [g];
          erw [ ZMod.cast_eq_val ];
          erw [ ZMod.val_cast_of_lt ];
          refine' lt_of_le_of_lt ( Finset.mem_Icc.mp hx |>.2 ) _;
          by_cases hn : n ≥ 1;
          · exact q_bound n hn;
          · interval_cases n ; simp_all;
            exact Fin.elim0 i;
        rw [ Finset.sum_congr rfl hg_def ];
        rw [ show A_n n = Finset.image ( fun x : ℕ => 6 * n ^ 2 - 2 * n + x ) ( Finset.range ( A_n n |> Finset.card ) ) from ?_, Finset.sum_image ] <;> norm_num;
        · rw [ Finset.card_image_of_injective _ fun x y hxy => by simpa using hxy ] ; norm_num [ Nat.cast_sub ( show 2 * n ≤ 6 * n ^ 2 by nlinarith ) ] ;
        · ext x
          simp [A_n];
          constructor <;> intro h;
          · exact ⟨ x - ( 6 * n ^ 2 - 2 * n ), by omega, by omega ⟩;
          · omega;
      have h_bound : ∀ r : ZMod (p n i), ∑ x ∈ Finset.range (L n), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = r then 1 else 0) ≤ (L n : ℝ) / (p n i) + 1 := by
        intro r
        have h_cycle : ∀ k : ℕ, ∑ x ∈ Finset.range (k * (p n i)), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = r then 1 else 0) ≤ k := by
          intro k
          have h_cycle : ∑ x ∈ Finset.range (p n i), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = r then 1 else 0) ≤ 1 := by
            haveI := Fact.mk ( show Nat.Prime ( p n i ) from by
                                have h_prime : p n i ∈ P_n n := by
                                  have h_prime : p n i ∈ primes_list n := by
                                    exact List.get_mem _ _
                                  generalize_proofs at *; (
                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 h_prime |> fun h => by simpa using h;)
                                generalize_proofs at *; (
                                exact Finset.mem_filter.mp h_prime |>.2.2) ) ; simp ;
            refine' Finset.card_le_one.mpr _;
            simp +zetaDelta at *;
            exact fun a ha₁ ha₂ b hb₁ hb₂ => Nat.mod_eq_of_lt ha₁ ▸ Nat.mod_eq_of_lt hb₁ ▸ by simpa [ ← ZMod.natCast_eq_natCast_iff' ] using ha₂.trans hb₂.symm;
          induction' k with k ih;
          · norm_num;
          · rw [ Nat.succ_mul, Finset.sum_range_add ];
            simp_all +decide [ add_comm ];
            linarith;
        have h_cycle : ∑ x ∈ Finset.range (L n), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = r then 1 else 0) ≤ (L n / (p n i) : ℕ) + 1 := by
          have h_cycle : ∑ x ∈ Finset.range (L n), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = r then 1 else 0) ≤ ∑ x ∈ Finset.range ((L n / (p n i) + 1) * (p n i)), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = r then 1 else 0) := by
            exact Finset.sum_le_sum_of_subset ( Finset.range_mono ( by linarith [ Nat.div_add_mod ( L n ) ( p n i ), Nat.mod_lt ( L n ) ( show p n i > 0 from Nat.Prime.pos ( by { have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by { exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) } ) ; aesop } ) ) ] ) );
          exact h_cycle.trans ( by solve_by_elim );
        refine le_trans ?_ ( add_le_add_right ( Nat.cast_div_le .. ) _ );
        exact_mod_cast h_cycle;
      have h_bound : ∑ x ∈ Finset.range (L n), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then 1 else 0) ≤ (∑ x ∈ Finset.range (L n), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = (r_K n i : ZMod (p n i)) then 1 else 0)) + (∑ x ∈ Finset.range (L n), (if (6 * n ^ 2 - 2 * n + x : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then 1 else 0)) := by
        simpa only [ ← Finset.sum_add_distrib ] using Finset.sum_le_sum fun x hx => by aesop;
      simp_all +decide [ mul_div_assoc ];
      linarith! [ ‹∀ r : ZMod ( p n i ), ( Finset.card ( Finset.filter ( fun x : ℕ => 6 * n ^ 2 - 2 * n + x = r ) ( Finset.range ( L n ) ) ) : ℝ ) ≤ ( L n : ℝ ) / p n i + 1› ( r_K n i ), ‹∀ r : ZMod ( p n i ), ( Finset.card ( Finset.filter ( fun x : ℕ => 6 * n ^ 2 - 2 * n + x = r ) ( Finset.range ( L n ) ) ) : ℝ ) ≤ ( L n : ℝ ) / p n i + 1› ( -r_K n i ), show ( Finset.card ( Finset.filter ( fun x : ℕ => 6 * n ^ 2 - 2 * n + x = ( r_K n i : ZMod ( p n i ) ) ∨ 6 * n ^ 2 - 2 * n + x = - ( r_K n i : ZMod ( p n i ) ) ) ( Finset.range ( L n ) ) ) : ℝ ) ≤ Finset.card ( Finset.filter ( fun x : ℕ => 6 * n ^ 2 - 2 * n + x = ( r_K n i : ZMod ( p n i ) ) ) ( Finset.range ( L n ) ) ) + Finset.card ( Finset.filter ( fun x : ℕ => 6 * n ^ 2 - 2 * n + x = - ( r_K n i : ZMod ( p n i ) ) ) ( Finset.range ( L n ) ) ) from mod_cast h_bound ]

/-
Helper Lemma: The number of elements in A_n congruent to a mod m is at most L/m + 1.
-/
lemma count_mod_bound (n : ℕ) (m : ℕ) (hm : m > 0) (a : ℕ) :
    ((A_n n).filter (fun x => x % m = a % m)).card ≤ (L n : ℝ) / m + 1 := by
      rw [ div_add_one, le_div_iff₀ ] <;> norm_cast <;> try positivity;
      -- Let $k$ be the number of elements in $A_n$ that are congruent to $a \pmod{m}$.
      set k := (Finset.filter (fun x => x % m = a % m) (A_n n)).card;
      -- Since these elements are distinct and lie within the interval $[6n^2 - 2n, 6n^2 + 10n + 3]$, we can order them as $x_1 < x_2 < \cdots < x_k$.
      obtain ⟨x_seq, hx_seq⟩ : ∃ x_seq : Fin k → ℕ, StrictMono x_seq ∧ ∀ i, x_seq i ∈ A_n n ∧ x_seq i % m = a % m := by
        exact ⟨ fun i => Finset.orderEmbOfFin _ ( by aesop ) i, by aesop_cat, fun i => Finset.mem_filter.mp ( Finset.orderEmbOfFin_mem _ ( by aesop ) i ) ⟩;
      have h_diff : ∀ i j : Fin k, i < j → x_seq j ≥ x_seq i + m := by
        intros i j hij
        have h_diff : (x_seq j) % m = (x_seq i) % m := by
          rw [ hx_seq.2 i |>.2, hx_seq.2 j |>.2 ];
        exact Nat.le_of_not_lt fun h => by have := Nat.modEq_iff_dvd.mp h_diff.symm; exact absurd this ( by exact fun ⟨ c, hc ⟩ => by nlinarith [ show c = 0 by nlinarith [ hx_seq.1 hij ], hx_seq.1 hij ] ) ;
      rcases k with ( _ | k ) <;> norm_num at *;
      have h_seq_bound : x_seq (Fin.last k) - x_seq 0 + 1 ≤ L n := by
        have h_seq_bound : x_seq (Fin.last k) - x_seq 0 + 1 ≤ Finset.card (Finset.Icc (x_seq 0) (x_seq (Fin.last k))) := by
          simp +arith +decide [ Nat.sub_add_comm ( show x_seq 0 ≤ x_seq ( Fin.last k ) from hx_seq.1.monotone ( Fin.zero_le _ ) ) ];
        exact h_seq_bound.trans ( Finset.card_le_card <| Finset.Icc_subset_Icc ( Finset.mem_Icc.mp ( hx_seq.2 0 |>.1 ) |>.1 ) ( Finset.mem_Icc.mp ( hx_seq.2 ( Fin.last k ) |>.1 ) |>.2 ) );
      have h_seq_bound : x_seq (Fin.last k) ≥ x_seq 0 + k * m := by
        have h_seq_bound : ∀ i : Fin (k + 1), x_seq i ≥ x_seq 0 + i * m := by
          exact fun i => Fin.inductionOn i ( by norm_num ) fun i hi => by have := h_diff _ _ ( Fin.castSucc_lt_succ i ) ; norm_num at * ; linarith;
        exact h_seq_bound _;
      linarith [ Nat.sub_add_cancel ( show x_seq ( Fin.last k ) ≥ x_seq 0 from hx_seq.1.monotone ( Fin.zero_le _ ) ) ]

/-
Helper Lemma: The number of elements in A_n satisfying x = a (mod p_i) and x = b (mod p_j) is at most L/(p_i p_j) + 1.
-/
lemma crt_intersection_bound (n : ℕ) (i j : Fin (w n)) (h_ne : i ≠ j) (a b : ℕ) :
    ((A_n n).filter (fun x => x % (p n i) = a % (p n i) ∧ x % (p n j) = b % (p n j))).card ≤ (L n : ℝ) / (p n i * p n j : ℝ) + 1 := by
      have h_coprime : Nat.Coprime (p n i) (p n j) := by
        have h_distinct : p n i ≠ p n j := by
          intro h_eq;
          have h_sorted : List.Nodup (primes_list n) := by
            exact Finset.sort_nodup _ _;
          have := List.nodup_iff_injective_get.mp h_sorted;
          exact h_ne ( by simpa [ Fin.ext_iff ] using this h_eq );
        have h_prime_i : Nat.Prime (p n i) := by
          have h_prime_i : p n i ∈ P_n n := by
            apply mem_P_n_iff_exists_index n (p n i) |>.2 ⟨i, rfl⟩
          exact (Finset.mem_filter.mp h_prime_i).right.right
        have h_prime_j : Nat.Prime (p n j) := by
          have h_prime_j : p n j ∈ P_n n := by
            exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ )
          exact (Finset.mem_filter.mp h_prime_j).right.right
        exact h_prime_i.coprime_iff_not_dvd.mpr (fun h => h_distinct <| Nat.prime_dvd_prime_iff_eq h_prime_i h_prime_j |>.1 h);
      obtain ⟨C, hC⟩ : ∃ C : ℕ, ∀ x : ℕ, x % p n i = a % p n i ∧ x % p n j = b % p n j ↔ x ≡ C [MOD p n i * p n j] := by
        have h_crt : ∃ C : ℕ, C ≡ a [MOD p n i] ∧ C ≡ b [MOD p n j] := by
          have := Nat.chineseRemainder h_coprime a b; aesop;
        obtain ⟨ C, hC₁, hC₂ ⟩ := h_crt; use C; intro x; rw [ ← Nat.modEq_and_modEq_iff_modEq_mul ] ; simp_all +decide [ Nat.ModEq ] ;
        assumption;
      have := @count_mod_bound n ( p n i * p n j ) ?_ C <;> norm_cast at * ; simp_all +decide [ Nat.ModEq ];
      exact mul_pos ( Nat.Prime.pos ( by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by { exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) } ) |>.2.2; aesop ) ) ( Nat.Prime.pos ( by have := Finset.mem_filter.mp ( show p n j ∈ P_n n from by { exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) } ) |>.2.2; aesop ) )

/-
Helper Lemma: The sum of g_i(x)g_j(x) is bounded by the sum of the counts of the four residue combinations.
-/
lemma cross_term_splitting (n : ℕ) (i j : Fin (w n)) :
    ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n j (x : ZMod (q n)) ≤
    ((A_n n).filter (fun x => x % (p n i) = (r_K n i) % (p n i) ∧ x % (p n j) = (r_K n j) % (p n j))).card +
    ((A_n n).filter (fun x => x % (p n i) = (r_K n i) % (p n i) ∧ x % (p n j) = (p n j - r_K n j) % (p n j))).card +
    ((A_n n).filter (fun x => x % (p n i) = (p n i - r_K n i) % (p n i) ∧ x % (p n j) = (r_K n j) % (p n j))).card +
    ((A_n n).filter (fun x => x % (p n i) = (p n i - r_K n i) % (p n i) ∧ x % (p n j) = (p n j - r_K n j) % (p n j))).card := by
      -- The sum of the products of the local hits is bounded by the sum of the counts of the four residue combinations because each term in the sum is either 0 or 1.
      have h_bound : ∀ x ∈ A_n n, (g n i x) * (g n j x) ≤ (if x % (p n i) = r_K n i % (p n i) ∧ x % (p n j) = r_K n j % (p n j) then 1 else 0) + (if x % (p n i) = r_K n i % (p n i) ∧ x % (p n j) = (p n j - r_K n j) % (p n j) then 1 else 0) + (if x % (p n i) = (p n i - r_K n i) % (p n i) ∧ x % (p n j) = r_K n j % (p n j) then 1 else 0) + (if x % (p n i) = (p n i - r_K n i) % (p n i) ∧ x % (p n j) = (p n j - r_K n j) % (p n j) then 1 else 0) := by
        unfold g;
        intro x hx; norm_cast; simp +decide [ ← ZMod.natCast_eq_natCast_iff' ] ;
        split_ifs <;> norm_num;
        simp_all +decide [ Nat.cast_sub ( show r_K n i ≤ p n i from Nat.div_le_of_le_mul <| by linarith [ show p n i ≥ 5 from by { have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by { exact ( mem_P_n_iff_exists_index n _ ) |>.2 ⟨ i, rfl ⟩ } ) ; linarith } ] ), Nat.cast_sub ( show r_K n j ≤ p n j from Nat.div_le_of_le_mul <| by linarith [ show p n j ≥ 5 from by { have := Finset.mem_filter.mp ( show p n j ∈ P_n n from by { exact ( mem_P_n_iff_exists_index n _ ) |>.2 ⟨ j, rfl ⟩ } ) ; linarith } ] ) ];
        rename_i h₁ h₂ h₃ h₄ h₅ h₆; rcases h₂ with ( h₂ | h₂ ) <;> rcases h₁ with ( h₁ | h₁ ) <;> simp_all +decide [ ZMod.natCast_eq_natCast_iff' ] ;
        · erw [ ZMod.cast_eq_val ] at * ; simp_all +decide [ ZMod.natCast_eq_natCast_iff' ] ;
          rw [ Nat.mod_mod_of_dvd _ ( show p n i ∣ q n from Finset.dvd_prod_of_mem _ <| by
                                        exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) |> fun h => Finset.mem_filter.mp h |>.1 |> fun h => Finset.mem_filter.mpr ⟨ h, by
                                          aesop ⟩
                                    ) ] at h₂ ; rw [ Nat.mod_mod_of_dvd _ ( show p n j ∣ q n from Finset.dvd_prod_of_mem _ <| by
                                                                                                                                          exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ] at h₁ ; aesop;
        · erw [ ZMod.cast_eq_val ] at * ; simp_all +decide [ ZMod.natCast_eq_natCast_iff' ] ;
          rw [ Nat.mod_mod_of_dvd _ ( show p n i ∣ q n from Finset.dvd_prod_of_mem _ <| by
                                        exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ] at h₂ ; simp_all ;
          rw [ Nat.mod_eq_of_lt ( show x < q n from by
                                    refine' lt_of_le_of_lt ( Finset.mem_Icc.mp hx |>.2 ) _;
                                    by_cases hn : n ≥ 1;
                                    · exact q_bound n hn;
                                    · interval_cases n ; simp_all;
                                      exact Fin.elim0 i ) ] at h₁ ; simp_all ;
        · erw [ ZMod.cast_eq_val ] at * ; simp_all +decide [ ZMod.natCast_eq_natCast_iff' ] ;
          rw [ Nat.mod_mod_of_dvd _ ( show p n j ∣ q n from Finset.dvd_prod_of_mem _ <| by { exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 <| by { exact List.get_mem _ _ } } ) ] at h₁ ; simp_all ;
          rw [ ← Nat.mod_add_div x ( q n ) ] at * ; simp_all ;
          exact h₅ ( mul_eq_zero_of_left ( by erw [ ZMod.natCast_eq_zero_iff ] ; exact Finset.dvd_prod_of_mem _ <| by { exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 <| by { exact List.get_mem _ _ } } ) _ );
        · convert h₆ _ _;
          · convert h₂ using 1;
            erw [ ZMod.cast_eq_val ];
            norm_num +zetaDelta at *;
            rw [ Nat.mod_eq_of_lt ];
            refine' lt_of_le_of_lt ( Finset.mem_Icc.mp hx |>.2 ) _;
            by_cases hn : n ≥ 1;
            · exact q_bound n hn;
            · interval_cases n ; simp_all ;
              exact Fin.elim0 i;
          · convert h₁ using 1;
            erw [ ZMod.cast_eq_val ];
            norm_num [ ZMod.natCast_zmod_val ];
            rw [ Nat.mod_eq_of_lt ( show x < q n from _ ) ];
            refine' lt_of_le_of_lt ( Finset.mem_Icc.mp hx |>.2 ) _;
            by_cases hn : n ≥ 1;
            · exact q_bound n hn;
            · interval_cases n ; simp_all ;
              exact Fin.elim0 i;
      convert Finset.sum_le_sum h_bound ; norm_cast ; simp +decide [ Finset.sum_add_distrib ]

/-
Lemma 2.4 (Cross-Term Density Bound): The number of simultaneous hits in the interval A_n for distinct primes p_i and p_j is bounded by 4L/(p_i p_j) + 4.
-/
theorem cross_term_density_bound (n : ℕ) (i j : Fin (w n)) (h_ne : i ≠ j) :
    ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n j (x : ZMod (q n)) ≤ (4 * L n : ℝ) / (p n i * p n j : ℝ) + 4 := by
  -- Apply the splitting lemma
  have h_split := cross_term_splitting n i j
  
  -- Apply the CRT intersection bound to each of the 4 terms
  have h1 := crt_intersection_bound n i j h_ne (r_K n i) (r_K n j)
  have h2 := crt_intersection_bound n i j h_ne (r_K n i) (p n j - r_K n j)
  have h3 := crt_intersection_bound n i j h_ne (p n i - r_K n i) (r_K n j)
  have h4 := crt_intersection_bound n i j h_ne (p n i - r_K n i) (p n j - r_K n j)

  -- Combine the bounds
  calc ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n j (x : ZMod (q n))
    _ ≤ _ := h_split
    _ ≤ ((L n : ℝ) / (p n i * p n j : ℝ) + 1) + ((L n : ℝ) / (p n i * p n j : ℝ) + 1) + ((L n : ℝ) / (p n i * p n j : ℝ) + 1) + ((L n : ℝ) / (p n i * p n j : ℝ) + 1) := by
      gcongr
    _ = 4 * ((L n : ℝ) / (p n i * p n j : ℝ) + 1) := by ring
    _ = (4 * L n : ℝ) / (p n i * p n j : ℝ) + 4 := by ring

/-
Define H(n) as the sum of the reciprocals of the primes in the sieve window.
-/
/-- Define $H(n)$ as the sum of the reciprocals of the primes in the sieve window:
$$ H(n) = \sum_{i=1}^w \frac{1}{p_i} $$ -/
noncomputable def H (n : ℕ) : ℝ :=
  ∑ i : Fin (w n), 1 / (p n i : ℝ)

/-
Lemma 3.2 (First Moment Bound): The First Moment is bounded by 2L * H(n) + 2w.
-/
/-- Lemma 3.2 (First Moment Bound):
By substituting the Local Hit Density Bound (Lemma 2.3) into the First Moment Expansion (Lemma 2.1), prove that the First Moment is bounded by:
$$ M_1(n) \le 2L \cdot H(n) + 2w $$ -/
theorem first_moment_bound (n : ℕ) :
    M_1 n ≤ 2 * (L n : ℝ) * H n + 2 * (w n : ℝ) := by
      rw [ H ];
      convert Finset.sum_le_sum fun i _ => local_hit_density_bound n i using 1;
      rw [ first_moment_expansion ];
      norm_num [ div_eq_mul_inv, Finset.mul_sum _ _ _, Finset.sum_add_distrib ] ; ring

/-
Lemma 3.3 (Second Moment Bound): The Second Moment is bounded by 4L * H(n)^2 + 4w^2 + M_1(n).
-/
/-- Lemma 3.3 (Second Moment Bound):
By substituting the Cross-Term Density Bound (Lemma 2.4) into the Second Moment Expansion (Lemma 2.2), and recognizing that the diagonal terms $i=j$ are identical to the local hits, prove that the Second Moment is strictly bounded by:
$$ M_2(n) \le 4L \cdot (H(n))^2 + 4w^2 + M_1(n) $$ -/
theorem second_moment_bound (n : ℕ) :
    M_2 n ≤ 4 * (L n : ℝ) * (H n)^2 + 4 * (w n : ℝ)^2 + M_1 n := by
      -- Split the sum into diagonal terms ($i=j$) and off-diagonal terms ($i \ne j$).
      have h_split : M_2 n = ∑ i : Fin (w n), ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n i (x : ZMod (q n)) + ∑ i : Fin (w n), ∑ j ∈ Finset.univ.erase i, ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n j (x : ZMod (q n)) := by
        simp +decide only [second_moment_expansion];
        simp;
      -- Since $g_i(x) \in \{0, 1\}$, $g_i(x)^2 = g_i(x)$. Thus the diagonal sum is exactly $M_1(n)$.
      have h_diag : ∑ i : Fin (w n), ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n i (x : ZMod (q n)) = M_1 n := by
        unfold M_1 ;
        unfold c; rw [ Finset.sum_comm ] ; congr; ext i; congr; ext x; unfold g; aesop;
      -- For the off-diagonal terms, use the Cross-Term Density Bound.
      have h_off_diag : ∑ i : Fin (w n), ∑ j ∈ Finset.univ.erase i, ∑ x ∈ A_n n, g n i (x : ZMod (q n)) * g n j (x : ZMod (q n)) ≤ ∑ i : Fin (w n), ∑ j ∈ Finset.univ.erase i, (4 * (L n : ℝ) / ((p n i) * (p n j) : ℝ) + 4) := by
        refine' Finset.sum_le_sum fun i hi => Finset.sum_le_sum fun j hj => _;
        convert cross_term_density_bound n i j ( by aesop ) using 1;
      -- The number of pairs $(i, j)$ with $i \ne j$ is $w(w-1) < w^2$, so the second term is bounded by $4w^2$.
      have h_off_diag_bound : ∑ i : Fin (w n), ∑ j ∈ Finset.univ.erase i, (4 * (L n : ℝ) / ((p n i) * (p n j) : ℝ) + 4) ≤ 4 * (L n : ℝ) * (∑ i : Fin (w n), ∑ j ∈ Finset.univ.erase i, (1 / ((p n i) * (p n j) : ℝ))) + 4 * (w n : ℝ) ^ 2 := by
        norm_num [ Finset.sum_add_distrib, Finset.mul_sum _ _ _, mul_assoc, div_eq_mul_inv ];
        norm_num [ ← Finset.mul_sum _ _ _, ← Finset.sum_mul ] ; ring_nf ; norm_num;
        cases k : w n <;> norm_num [ k ] ; nlinarith;
      -- For the first term, note that $\sum_{i \ne j} \frac{1}{p_i p_j} < \left( \sum_{i=1}^w \frac{1}{p_i} \right)^2 = H(n)^2$.
      have h_off_diag_first_term : ∑ i : Fin (w n), ∑ j ∈ Finset.univ.erase i, (1 / ((p n i) * (p n j) : ℝ)) ≤ (∑ i : Fin (w n), (1 / ((p n i) : ℝ))) ^ 2 := by
        norm_num [ sq, Finset.sum_mul _ _ _ ];
        norm_num [ ← Finset.mul_sum _ _ _, ← Finset.sum_mul ];
        exact Finset.sum_nonneg fun _ _ => mul_self_nonneg _;
      unfold H; nlinarith;

/-
Define the total weighted mass of the interval S_1(n) as the sum of W(x) for x in A_n.
-/
/-- Define the total weighted mass of the interval $S_1(n)$:
$$ S_1(n) = \sum_{x \in \mathcal{A}_n} W(x) $$ -/
noncomputable def S_1 (n : ℕ) (W : ZMod (q n) → ℝ) : ℝ :=
  ∑ x ∈ A_n n, W (x : ZMod (q n))

/-
Define the weighted hit count S_2(n) as the sum of W(x)c(x) for x in A_n.
-/
/-- Define the weighted hit count $S_2(n)$:
$$ S_2(n) = \sum_{x \in \mathcal{A}_n} W(x) c(x) $$ -/
noncomputable def S_2 (n : ℕ) (W : ZMod (q n) → ℝ) : ℝ :=
  ∑ x ∈ A_n n, W (x : ZMod (q n)) * c n (x : ZMod (q n))

/-
Prove that c(x) >= 0 and if c(x) < 1 then c(x) = 0.
-/
/-- Lemma 4.4 (Non-negative Hits):
Using the definition of $g_i(x)$ and $c(x)$, prove that for any $x$, the hit counter is non-negative: $c(x) \ge 0$. Furthermore, note that because $c(x)$ is a sum of indicator functions, if $c(x) < 1$, then $c(x) = 0$. -/
lemma non_negative_hits (n : ℕ) (x : ZMod (q n)) :
    c n x ≥ 0 ∧ (c n x < 1 → c n x = 0) := by
      unfold c;
      unfold g; aesop;

/-
If S_2(n) < S_1(n), then there exists x in A_n such that W(x) > 0 and c(x) = 0.
-/
/-- Theorem 4.5 (The Weighted Existence Principle):
Assume there exists a specific configuration such that $S_2(n) < S_1(n)$. Prove that there must exist at least one integer $x \in \mathcal{A}_n$ such that $W(x) > 0$ and $c(x) = 0$. -/
theorem weighted_existence_principle (n : ℕ) (W : ZMod (q n) → ℝ) (hW : ∀ x, W x ≥ 0)
    (h_ineq : S_2 n W < S_1 n W) :
    ∃ x ∈ A_n n, W (x : ZMod (q n)) > 0 ∧ c n (x : ZMod (q n)) = 0 := by
      contrapose! h_ineq;
      -- For each $x \in A_n$, if $W(x) > 0$ then $c(x) \ge 1$.
      have h_c_ge_one : ∀ x ∈ A_n n, W x > 0 → c n x ≥ 1 := by
        -- Since $c(x)$ is the sum of non-negative terms, if $c(x) \neq 0$, then $c(x) \geq 1$.
        intros x hx hWx_pos
        have h_c_nonneg : 0 ≤ c n x := by
          exact Finset.sum_nonneg fun _ _ => by unfold g; split_ifs <;> norm_num;
        contrapose! h_ineq;
        exact ⟨ x, hx, hWx_pos, by linarith [ show c n x = 0 from by linarith [ show c n x = 0 from by exact non_negative_hits n x |>.2 <| by linarith ] ] ⟩;
      -- Therefore, for all $x \in A_n$, $W(x) c(x) \ge W(x)$.
      have h_Wc_ge_W : ∀ x ∈ A_n n, W x * c n (x : ZMod (q n)) ≥ W x := by
        exact fun x hx => if hx' : W x = 0 then by simp +decide [ hx' ] else by nlinarith [ hW x, h_c_ge_one x hx ( lt_of_le_of_ne ( hW x ) ( Ne.symm hx' ) ) ] ;
      exact Finset.sum_le_sum h_Wc_ge_W

/-
Definition 5.1 (Fourier Transform of the Weight):
Define $\hat{W}(h)$ as the Discrete Fourier Transform of the weight function $W(x)$ over $\mathbb{Z}/q\mathbb{Z}$:
$$ \hat{W}(h) = \frac{1}{q} \sum_{x=0}^{q-1} W(x) e^{-2\pi i h x / q} $$
-/
noncomputable def W_hat (n : ℕ) (W : ZMod (q n) → ℝ) (h : ZMod (q n)) : ℂ :=
  haveI : NeZero (q n) := ⟨by
    exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2⟩
  (1 / (q n : ℂ)) * ∑ x : ZMod (q n), (W x : ℂ) * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ))

/-
Definition 5.2 (Fourier Transform of the Local Hit):
Define $\hat{g}_i(h)$ as the Discrete Fourier Transform of the local hit function $g_i(x)$:
$$ \hat{g}_i(h) = \frac{1}{q} \sum_{x=0}^{q-1} g_i(x) e^{-2\pi i h x / q} $$
-/
noncomputable def g_hat (n : ℕ) (i : Fin (w n)) (h : ZMod (q n)) : ℂ :=
  haveI : NeZero (q n) := ⟨by
    exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2⟩
  (1 / (q n : ℂ)) * ∑ x : ZMod (q n), (g n i x : ℂ) * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ))

/-
Lemma: Given the compact support of $W$ in $\mathcal{A}_n$, the sum of $W(x)$ over $\mathbb{Z}/q\mathbb{Z}$ is equal to $S_1(n)$.
-/
lemma sum_W_eq_S1 (n : ℕ) (hn : n ≥ 1) (W : ZMod (q n) → ℝ)
    (h_supp : ∀ x : ZMod (q n), x.val ∉ A_n n → W x = 0) :
    ∑ x : ZMod (q n), W x = S_1 n W := by
      rw [ ← Finset.sum_subset ( show Finset.image ( fun x ↦ x : ℕ → ZMod ( q n ) ) ( A_n n ) ⊆ Finset.univ from Finset.subset_univ _ ) ];
      · refine' Finset.sum_bij ( fun x hx => x.val ) _ _ _ _ <;> simp;
        · intro x hx; rw [ Nat.mod_eq_of_lt ] ; aesop;
          exact lt_of_le_of_lt ( Finset.mem_Icc.mp hx |>.2 ) ( q_bound n hn );
        · intro a ha b hb hab; erw [ ZMod.natCast_eq_natCast_iff ] ; aesop;
        · intro b hb;
          refine' ⟨ b, hb, Nat.mod_eq_of_lt _ ⟩;
          exact lt_of_le_of_lt ( Finset.mem_Icc.mp hb |>.2 ) ( q_bound n hn );
      · intro x hx; contrapose! h_supp; aesop;

/-
Lemma 5.3 (Compact Support Equivalence):
Assume the weight function is strictly supported inside the interval: $\forall x \notin \mathcal{A}_n, W(x) = 0$. Prove that the interval sum $S_1(n)$ is equivalent to the full space sum, which yields the zero-frequency Fourier coefficient:
$$ S_1(n) = \sum_{x \in \mathbb{Z}/q\mathbb{Z}} W(x) = q \hat{W}(0) $$
-/
lemma compact_support_equivalence (n : ℕ) (hn : n ≥ 1) (W : ZMod (q n) → ℝ)
    (h_supp : ∀ x : ZMod (q n), x.val ∉ A_n n → W x = 0) :
    S_1 n W = (q n : ℝ) * (W_hat n W 0).re := by
      rw [ ← sum_W_eq_S1 ];
      · unfold W_hat; norm_num; ring_nf; aesop;
      · assumption;
      · assumption

/-
Lemma: Orthogonality of the exponential sum with negative sign.
$$ \sum_{h=0}^{q-1} e^{-2\pi i h k / q} = \begin{cases} q & \text{if } k = 0 \\ 0 & \text{if } k \neq 0 \end{cases} $$
-/
lemma sum_exp_orthogonality_neg (n : ℕ) (k : ZMod (q n)) :
    ∑ h : ZMod (q n), Complex.exp (-2 * Real.pi * Complex.I * h.val * k.val / (q n : ℂ)) = if k = 0 then (q n : ℂ) else 0 := by
      have h_conj : ∑ h : ZMod (q n), Complex.exp (-2 * Real.pi * Complex.I * h.val * k.val / (q n : ℂ)) = starRingEnd ℂ (∑ h : ZMod (q n), Complex.exp (2 * Real.pi * Complex.I * h.val * k.val / (q n : ℂ))) := by
        rw [ map_sum ] ; congr ; ext ; rw [ ← Complex.exp_conj ] ; ring_nf;
        norm_num [ Complex.ext_iff, Complex.exp_re, Complex.exp_im ];
        erw [ ZMod.cast_eq_val, ZMod.cast_eq_val ] ; norm_cast ; ring_nf ; norm_num;
      convert h_conj using 1;
      rw [ sum_exp_orthogonality ] ; aesop

/-
Plancherel's Theorem for the custom Fourier transform definition:
$$ \sum_{x} f(x) \overline{g(x)} = q \sum_{h} \hat{f}(h) \overline{\hat{g}(h)} $$
-/
lemma plancherel_theorem_custom (n : ℕ) (f g : ZMod (q n) → ℂ) :
    ∑ x : ZMod (q n), f x * starRingEnd ℂ (g x) =
    (q n : ℂ) * ∑ h : ZMod (q n), (1 / (q n : ℂ) * ∑ x, f x * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ))) *
    starRingEnd ℂ (1 / (q n : ℂ) * ∑ x, g x * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ))) := by
      -- Apply the Plancherel theorem to the functions $f$ and $g$.
      have h_plancherel : ∑ x : ZMod (q n), f x * (starRingEnd ℂ (g x)) = ∑ h : ZMod (q n), (∑ x : ZMod (q n), f x * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val) / (q n : ℂ))) * (∑ y : ZMod (q n), (starRingEnd ℂ (g y)) * Complex.exp (2 * Real.pi * Complex.I * (h.val * y.val) / (q n : ℂ))) / (q n : ℂ) := by
        have h_ortho : ∀ x y : ZMod (q n), (∑ h : ZMod (q n), Complex.exp (-2 * Real.pi * Complex.I * (h.val * (x.val - y.val)) / (q n : ℂ))) = if x = y then (q n : ℂ) else 0 := by
          intro x y; split_ifs with hxy; simp_all ;
          have h_ortho : ∑ h ∈ Finset.range (q n), Complex.exp (-2 * Real.pi * Complex.I * (h * (x.val - y.val)) / (q n)) = 0 := by
            -- Let $z = e^{-2\pi i (x.val - y.val) / q_n}$. Since $x \neq y$, $z$ is a primitive $q_n$-th root of unity.
            set z : ℂ := Complex.exp (-2 * Real.pi * Complex.I * (x.val - y.val) / (q n))
            have hz : z ≠ 1 := by
              rw [ Ne.eq_def, Complex.exp_eq_one_iff ];
              field_simp;
              intro h; obtain ⟨ k, hk ⟩ := h; rw [ neg_eq_iff_eq_neg ] at hk; rw [ div_eq_iff ] at hk <;> norm_cast at * <;> simp_all ;
              · rw [ Int.subNatNat_eq_coe ] at hk ; replace hk := congr_arg ( fun z => z : ℤ → ZMod ( q n ) ) hk ; simp_all ;
                exact hxy <| sub_eq_zero.mp hk;
              · exact Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2
            have hz_pow : ∑ h ∈ Finset.range (q n), z ^ h = 0 := by
              rw [ geom_sum_eq ] <;> norm_num [ hz ];
              rw [ ← Complex.exp_nat_mul, mul_comm, Complex.exp_eq_one_iff.mpr ⟨ - ( x.val - y.val ), by push_cast; ring_nf; norm_num [ show q n ≠ 0 from Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2 ] ⟩ ] ; norm_num;
            exact Eq.trans ( Finset.sum_congr rfl fun _ _ => by rw [ ← Complex.exp_nat_mul ] ; ring_nf ) hz_pow;
          convert h_ortho using 1;
          refine' Finset.sum_bij ( fun h _ => h.val ) _ _ _ _ <;> simp +decide [ ZMod.val ];
          · cases h : q n <;> simp_all +decide [ ZMod ];
            exact absurd h <| ne_of_gt <| Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2;
          · rcases k : q n with ( _ | _ | k ) <;> simp_all +decide [ ZMod ];
            · exact absurd k <| ne_of_gt <| Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2;
            · exact fun a₁ a₂ h => Fin.ext h;
          · cases h : q n <;> simp_all +decide [ ZMod ];
            exact fun b hb => ⟨ ⟨ b, by linarith ⟩, rfl ⟩;
        -- By Fubini's theorem, we can interchange the order of summation.
        have h_fubini : ∑ h : ZMod (q n), (∑ x : ZMod (q n), f x * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val) / (q n : ℂ))) * (∑ y : ZMod (q n), (starRingEnd ℂ (g y)) * Complex.exp (2 * Real.pi * Complex.I * (h.val * y.val) / (q n : ℂ))) = ∑ x : ZMod (q n), ∑ y : ZMod (q n), f x * (starRingEnd ℂ (g y)) * (∑ h : ZMod (q n), Complex.exp (-2 * Real.pi * Complex.I * (h.val * (x.val - y.val)) / (q n : ℂ))) := by
          simp +decide only [Finset.sum_mul _ _ _, Finset.mul_sum];
          rw [ Finset.sum_comm ];
          exact Finset.sum_congr rfl fun _ _ => Finset.sum_comm.trans ( Finset.sum_congr rfl fun _ _ => Finset.sum_congr rfl fun _ _ => by rw [ mul_mul_mul_comm ] ; rw [ ← Complex.exp_add ] ; ring_nf );
        simp_all +decide [ ← Finset.sum_div _ _ _, ← Finset.sum_mul ];
      rw [ h_plancherel, Finset.mul_sum _ _ _ ] ; congr ; ext ; ring_nf;
      by_cases h : q n = 0 <;> simp +decide [ h, mul_assoc, mul_comm, mul_left_comm ];
      norm_num [ Complex.ext_iff, Complex.exp_re, Complex.exp_im ];
      left; ring_nf;
      constructor <;> congr! 1;
      · erw [ ZMod.cast_eq_val, ZMod.cast_eq_val ] ; ring_nf;
        erw [ Complex.ofReal_re, Complex.ofReal_im ] ; ring_nf;
      · erw [ ZMod.cast_eq_val, ZMod.cast_eq_val ] ; ring_nf;
        norm_cast ; ring_nf

/-
Lemma 5.4 (The Plancherel Hit Expansion):
Using the compact support of $W(x)$ and admitting Plancherel's theorem for the inner product of $W(x)$ and $g_i(x)$, expand the weighted hit count $S_2(n)$ into the Fourier domain:
$$ S_2(n) = q \sum_{i=1}^w \sum_{h=0}^{q-1} \hat{W}(h) \overline{\hat{g}_i(h)} $$
-/
lemma plancherel_hit_expansion (n : ℕ) (hn : n ≥ 1) (W : ZMod (q n) → ℝ)
    (h_supp : ∀ x : ZMod (q n), x.val ∉ A_n n → W x = 0) :
    (S_2 n W : ℂ) = (q n : ℂ) * ∑ i : Fin (w n), ∑ h : ZMod (q n), W_hat n W h * starRingEnd ℂ (g_hat n i h) := by
      convert plancherel_theorem_custom n ( fun x => W x ) ( fun x => ∑ i : Fin ( w n ), g n i x ) using 1;
      · convert rfl using 2;
        unfold S_2; norm_num [ Finset.sum_mul _ _ _ ] ; ring_nf;
        convert sum_W_eq_S1 n hn ( fun x => W x * ∑ i, g n i x ) _ using 1;
        · norm_cast;
        · aesop;
      · simp +decide [ W_hat, g_hat, Finset.mul_sum _ _ _, Finset.sum_mul ];
        exact Finset.sum_comm.trans ( Finset.sum_congr rfl fun _ _ => Finset.sum_comm.trans ( Finset.sum_congr rfl fun _ _ => by ac_rfl ) )

/-
Lemma 6.1a (Dirac Comb Non-Zero Values):
For a frequency $h$ that is a multiple of $q/p_i$, specifically $h = k(q/p_i)$, the Fourier transform evaluates to:
$$ \hat{g}_i\left(k \frac{q}{p_i}\right) = \frac{2}{p_i} \cos\left( \frac{2\pi k r^K_i}{p_i} \right) $$
-/
lemma dirac_comb_nonzero (n : ℕ) (i : Fin (w n)) (k : Fin (p n i)) :
    g_hat n i (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n)) =
    ((2 : ℝ) / (p n i : ℝ) * Real.cos (2 * Real.pi * (k : ℝ) * (r_K n i : ℝ) / (p n i : ℝ)) : ℂ) := by
      -- By definition of $g_i$, we know that $\hat{g}_i(h) = \frac{2}{p_i} \cos(2\pi k r^K_i / p_i)$ for $h = k(q/p_i)$.
      have h_g_hat_i : ∀ h : ZMod (q n), h.val = k.val * (q n / p n i) → g_hat n i h = (1 / (q n : ℂ)) * ∑ x ∈ Finset.range (q n), (if (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) then 1 else 0) * Complex.exp (-2 * Real.pi * Complex.I * (h.val * x : ℕ) / (q n : ℂ)) := by
        intro h hh; unfold g_hat; norm_num [ ZMod.cast_id, ZMod.natCast_zmod_val, hh ] ;
        refine' Or.inl ( Finset.sum_bij ( fun x _ => x.val ) _ _ _ _ ) <;> norm_num;
        · intro a; exact ZMod.val_lt a;
        · intro a₁ a₂ h; haveI := Fact.mk ( show Nat.Prime 2 from by decide ) ; exact ZMod.val_injective _ h;
        · exact fun b hb => ⟨ b, ZMod.val_cast_of_lt hb ⟩;
        · unfold g; aesop;
      -- Let's simplify the sum $\sum_{x=0}^{q_n-1} \mathbf{1}_{\{x \equiv \pm r^K_i \pmod{p_n}\}} e^{-2\pi i k x / p_n}$.
      have h_sum_simplified : ∑ x ∈ Finset.range (q n), (if (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) then 1 else 0) * Complex.exp (-2 * Real.pi * Complex.I * (k.val * (q n / p n i) * x) / (q n : ℂ)) = ∑ x ∈ Finset.range (p n i), (if (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) then 1 else 0) * Complex.exp (-2 * Real.pi * Complex.I * (k.val * x) / (p n i : ℂ)) * (q n / p n i) := by
        have h_sum_simplified : ∀ m : ℕ, ∑ x ∈ Finset.range (m * p n i), (if (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) then 1 else 0) * Complex.exp (-2 * Real.pi * Complex.I * (k.val * (q n / p n i) * x) / (q n : ℂ)) = ∑ x ∈ Finset.range (p n i), (if (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) then 1 else 0) * Complex.exp (-2 * Real.pi * Complex.I * (k.val * x) / (p n i : ℂ)) * m := by
          intro m
          induction' m with m ih;
          · norm_num;
          · rw [ Nat.succ_mul, Finset.sum_range_add, ih ];
            simp +decide [ Finset.sum_add_distrib, mul_add ];
            refine' Finset.sum_congr rfl fun x hx => _ ; split_ifs <;> ring_nf;
            by_cases h : p n i = 0 <;> simp_all +decide [ mul_assoc, mul_comm, mul_left_comm ];
            exact Complex.exp_eq_exp_iff_exists_int.mpr ⟨ -m * k, by push_cast; ring ⟩;
        convert h_sum_simplified ( q n / p n i ) using 1;
        · rw [ Nat.div_mul_cancel ];
          exact Finset.dvd_prod_of_mem _ ( Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( show p n i < 6 * n + 2 from by
                                                                                            have := Finset.mem_range.mp ( Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.1 ) ; aesop; ), by
                                                                                            have h_prime : p n i ∈ P_n n := by
                                                                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                                                            exact ⟨ Finset.mem_filter.mp h_prime |>.2.1, Finset.mem_filter.mp h_prime |>.2.2 ⟩ ⟩ );
        · rw [ Nat.cast_div ];
          · exact Finset.dvd_prod_of_mem _ ( Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( show p n i < 6 * n + 2 from by
                                                                                              have := Finset.mem_range.mp ( Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                                                    exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.1 ) ; aesop; ), by
                                                                                              have h_prime : p n i ∈ P_n n := by
                                                                                                exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                                                              exact ⟨ Finset.mem_filter.mp h_prime |>.2.1, Finset.mem_filter.mp h_prime |>.2.2 ⟩ ⟩ );
          · exact Nat.cast_ne_zero.mpr <| Nat.Prime.ne_zero <| by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop;
      -- Let's simplify the sum $\sum_{x=0}^{p_n-1} \mathbf{1}_{\{x \equiv \pm r^K_i \pmod{p_n}\}} e^{-2\pi i k x / p_n}$.
      have h_sum_final : ∑ x ∈ Finset.range (p n i), (if (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) then 1 else 0) * Complex.exp (-2 * Real.pi * Complex.I * (k.val * x) / (p n i : ℂ)) = Complex.exp (-2 * Real.pi * Complex.I * (k.val * r_K n i) / (p n i : ℂ)) + Complex.exp (2 * Real.pi * Complex.I * (k.val * r_K n i) / (p n i : ℂ)) := by
        have h_sum_final : ∑ x ∈ Finset.range (p n i), (if (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) then 1 else 0) * Complex.exp (-2 * Real.pi * Complex.I * (k.val * x) / (p n i : ℂ)) = ∑ x ∈ ({r_K n i, (p n i - r_K n i) % (p n i)} : Finset ℕ), Complex.exp (-2 * Real.pi * Complex.I * (k.val * x) / (p n i : ℂ)) := by
          have h_sum_final : ∀ x ∈ Finset.range (p n i), (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) ↔ x = r_K n i ∨ x = (p n i - r_K n i) % (p n i) := by
            intro x hx
            have h_cong : (x : ZMod (p n i)) = r_K n i ∨ (x : ZMod (p n i)) = -(r_K n i) ↔ x ≡ r_K n i [MOD p n i] ∨ x ≡ p n i - r_K n i [MOD p n i] := by
              simp +decide [ ← ZMod.natCast_eq_natCast_iff, Nat.cast_sub ( show r_K n i ≤ p n i from Nat.div_le_of_le_mul <| by linarith [ show p n i ≥ 5 from by
                                                                                                                                            have h_prime : p n i ∈ P_n n := by
                                                                                                                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ )
                                                                                                                                            exact (Finset.mem_filter.mp h_prime).right.left ] ) ];
            simp_all +decide [ Nat.ModEq, Nat.mod_eq_of_lt ];
            rw [ Nat.mod_eq_of_lt ];
            exact Nat.div_lt_of_lt_mul <| by linarith [ show p n i ≥ 5 from by
                                                          have h_prime : p n i ∈ P_n n := by
                                                            exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                          exact Finset.mem_filter.mp h_prime |>.2.1 ] ;
          rw [ ← Finset.sum_subset ( show { r_K n i, ( p n i - r_K n i ) % p n i } ⊆ Finset.range ( p n i ) from ?_ ) ];
          · refine' Finset.sum_congr rfl fun x hx => _;
            simp +zetaDelta at *;
            exact fun h₁ h₂ => False.elim <| h₁ <| by specialize h_sum_final x ( by
              exact hx.elim ( fun hx => hx.symm ▸ Nat.div_lt_of_lt_mul ( by linarith [ Nat.Prime.two_le ( show Nat.Prime ( p n i ) from by
                                                                                                            grind ) ] ) ) fun hx => hx.symm ▸ Nat.mod_lt _ ( Nat.Prime.pos ( show Nat.Prime ( p n i ) from by
                                                                                                                                                                                                              exact Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                                                                                                                                            exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.2.2 ) ) ) ; aesop;
          · grind +ring;
          · simp +decide [ Finset.insert_subset_iff ];
            exact ⟨ Nat.div_lt_of_lt_mul <| by linarith [ show p n i ≥ 5 from by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; exact this.2.1 ], Nat.mod_lt _ <| Nat.Prime.pos <| by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                                                                                                                                            exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; exact this.2.2 ⟩;
        rw [ h_sum_final, Finset.sum_pair ];
        · rw [ Nat.mod_eq_of_lt ];
          · rw [ Nat.cast_sub ( show r_K n i ≤ p n i from _ ) ] ; ring_nf;
            · norm_num [ show p n i ≠ 0 by exact Nat.ne_of_gt ( Nat.Prime.pos ( by exact Finset.mem_filter.mp ( Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( show p n i ∈ Finset.sort ( α := ℕ ) ( · ≤ · ) ( Finset.filter ( fun p => 5 ≤ p ∧ p.Prime ) ( Finset.range ( 6 * n + 2 ) ) ) from List.get_mem _ _ ) ) |>.2.2 ) ) ];
              exact Complex.exp_eq_exp_iff_exists_int.mpr ⟨ -k, by push_cast; ring ⟩;
            · exact Nat.div_le_of_le_mul <| by linarith [ show p n i ≥ 5 from by
                                                            have h_prime : p n i ∈ P_n n := by
                                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                            exact Finset.mem_filter.mp h_prime |>.2.1 ] ;
          · refine' Nat.sub_lt _ _;
            · exact Nat.Prime.pos ( by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                        exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop );
            · exact Nat.div_pos ( by linarith [ show p n i ≥ 5 from by
                                                  have h_prime : p n i ∈ P_n n := by
                                                    exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                  exact Finset.mem_filter.mp h_prime |>.2.1 ] ) ( by norm_num );
        · rw [ Nat.mod_eq_of_lt ];
          · unfold r_K;
            have h_pi_ge_5 : 5 ≤ p n i := by
              have h_prime_ge_5 : ∀ i : Fin (w n), 5 ≤ p n i := by
                intro i
                have h_prime_ge_5 : p n i ∈ P_n n := by
                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ )
                exact (Finset.mem_filter.mp h_prime_ge_5).right.left;
              exact h_prime_ge_5 i;
            omega;
          · refine' Nat.sub_lt _ _;
            · exact Nat.Prime.pos ( by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                        exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop );
            · exact Nat.div_pos ( by linarith [ show p n i ≥ 5 from by
                                                  have h_prime : p n i ∈ P_n n := by
                                                    exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                  exact Finset.mem_filter.mp h_prime |>.2.1 ] ) ( by norm_num );
      convert h_g_hat_i _ _ using 1;
      · convert congr_arg ( fun x : ℂ => ( 1 / ( q n : ℂ ) ) * x ) h_sum_simplified using 1;
        · rw [ h_sum_simplified, ← Finset.sum_mul _ _ _ ] ; rw [ h_sum_final ] ; norm_num [ Complex.cos ] ; ring_nf;
          norm_num [ show q n ≠ 0 from Finset.prod_ne_zero_iff.mpr fun p hp => Nat.Prime.ne_zero <| Finset.mem_filter.mp hp |>.2.2 ];
        · convert congr_arg ( fun x : ℂ => ( 1 / ( q n : ℂ ) ) * x ) h_sum_simplified using 1;
          norm_num [ ZMod.val_mul, ZMod.val_natCast ];
          rw [ Nat.mod_eq_of_lt ];
          · rw [ Nat.cast_mul, Nat.cast_div ] <;> norm_num;
            · exact Finset.dvd_prod_of_mem _ ( Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( show p n i < 6 * n + 2 from by
                                                                                                have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                                exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop; ), by
                                                                                                exact Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.2 ⟩ );
            · exact Nat.Prime.ne_zero ( by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by { exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) } ) ; aesop );
          · refine' lt_of_lt_of_le ( mul_lt_mul_of_pos_right ( Fin.is_lt k ) ( Nat.div_pos _ _ ) ) _;
            · refine' Nat.le_of_dvd ( Finset.prod_pos fun p hp => Nat.Prime.pos ( Finset.mem_filter.mp hp |>.2.2 ) ) ( Finset.dvd_prod_of_mem _ _ );
              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
            · exact Fin.pos k;
            · exact Nat.mul_div_le _ _;
      · norm_num [ ZMod.val_mul ];
        rw [ Nat.mod_eq_of_lt ];
        refine' lt_of_lt_of_le ( mul_lt_mul_of_pos_right ( Fin.is_lt k ) ( Nat.div_pos _ _ ) ) _;
        · exact Nat.le_of_dvd ( Finset.prod_pos fun x hx => Nat.Prime.pos ( Finset.mem_filter.mp hx |>.2.2 ) ) ( Finset.dvd_prod_of_mem _ ( Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( show p n i < 6 * n + 2 from by
                                                                                                                                                                                            have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                                                                                                                            exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop; ), by
                                                                                                                                                                                            exact Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                                                                                                                                                                          exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.2 ⟩ ) );
        · exact Nat.Prime.pos ( by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by
                                                                    exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; exact this.2.2 );
        · exact Nat.mul_div_le _ _

/-
Lemma 6.1b (Dirac Comb Zero Values):
For any frequency $h$ that is NOT a multiple of $q/p_i$, the Fourier transform of the local hit function is zero:
$$ \hat{g}_i(h) = 0 $$
-/
lemma dirac_comb_zero (n : ℕ) (i : Fin (w n)) (h : ZMod (q n))
    (h_not_multiple : ∀ k : Fin (p n i), h ≠ (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n))) :
    g_hat n i h = 0 := by
      -- The inner sum is a geometric series $\sum_{m=0}^{M-1} (\zeta^h)^m$ where $\zeta = e^{-2\pi i p_i / q}$ is a primitive $M$-th root of unity, with $M = q/p_i$.
      have h_geo_series : ∑ m ∈ Finset.range (q n / (p n i)), Complex.exp (-2 * Real.pi * Complex.I * h.val * m * (p n i) / (q n)) = 0 := by
        -- Let $M = q / p_i$. Since $h$ is not a multiple of $q / p_i$, $\zeta^h \neq 1$.
        set M := q n / p n i
        have h_zeta_ne_one : Complex.exp (-2 * Real.pi * Complex.I * h.val * (p n i) / (q n)) ≠ 1 := by
          -- Since $h$ is not a multiple of $q/p_i$, $h.val * p n i$ is not a multiple of $q n$.
          have h_not_div : ¬(q n ∣ h.val * p n i) := by
            contrapose! h_not_multiple;
            -- Since $q n \mid h.val * p n i$, we can write $h.val = k * (q n / p n i)$ for some integer $k$.
            obtain ⟨k, hk⟩ : ∃ k : ℕ, h.val = k * (q n / p n i) := by
              refine' exists_eq_mul_left_of_dvd _;
              refine' Nat.dvd_of_mul_dvd_mul_right ( show p n i > 0 from Nat.Prime.pos <| by
                                                      have h_prime : p n i ∈ P_n n := by
                                                        have h_perm : ∀ x ∈ primes_list n, x ∈ P_n n := by
                                                          exact fun x hx => Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 hx
                                                        exact h_perm _ ( List.get_mem _ _ );
                                                      exact Finset.mem_filter.mp h_prime |>.2.2 ) _;
              rwa [ Nat.div_mul_cancel ( show p n i ∣ q n from Finset.dvd_prod_of_mem _ <| Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr <| by linarith [ Finset.mem_range.mp <| Finset.mem_filter.mp ( show p n i ∈ Finset.filter ( fun p => 5 ≤ p ∧ p.Prime ) ( Finset.range ( 6 * n + 2 ) ) from by
                                                                                                                                                                                                        exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.1 ], Finset.mem_filter.mp ( show p n i ∈ Finset.filter ( fun p => 5 ≤ p ∧ p.Prime ) ( Finset.range ( 6 * n + 2 ) ) from by
                                                                                                                                                                                                                                                                                                                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) |>.2 ⟩ ) ];
            use ⟨k % (p n i), by
              exact Nat.mod_lt _ ( Nat.Prime.pos ( by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by { exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 <| List.get_mem _ _ } ) ; aesop ) )⟩
            generalize_proofs at *;
            simp +zetaDelta at *;
            convert congr_arg ( fun x : ℕ => x : ℕ → ZMod ( q n ) ) hk using 1;
            · convert rfl;
              convert ZMod.natCast_zmod_val h;
            · rw [ ← Nat.mod_add_div k ( p n i ) ] ; norm_num [ Nat.add_mul, Nat.mul_mod, Nat.mod_eq_of_lt ‹_› ] ;
              norm_cast;
              rw [ mul_right_comm, Nat.mul_div_cancel' ];
              · rw [ ZMod.natCast_eq_zero_iff ] ; exact dvd_mul_right _ _;
              · exact Finset.dvd_prod_of_mem _ ( Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( by linarith [ Fin.is_lt i, show p n i ≤ 6 * n + 1 from by
                                                                                                                            have h_pi_le : p n i ∈ Finset.filter (fun p => 5 ≤ p ∧ p.Prime) (Finset.range (6 * n + 2)) := by
                                                                                                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                                                                                            grind ] ), by
                                                                                                                            have h_prime : p n i ∈ P_n n := by
                                                                                                                              exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                                                                                            exact ⟨ Finset.mem_filter.mp h_prime |>.2.1, Finset.mem_filter.mp h_prime |>.2.2 ⟩ ⟩ );
          rw [ Ne.eq_def, Complex.exp_eq_one_iff ];
          field_simp;
          exact fun ⟨ k, hk ⟩ => h_not_div <| Int.natCast_dvd_natCast.mp <| ⟨ -k, by push_cast [ ← @Int.cast_inj ℂ ] ; rw [ neg_div', div_eq_iff <| Nat.cast_ne_zero.mpr <| by linarith [ show 0 < q n from Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2 ] ] at *; linear_combination hk.symm ⟩;
        have h_geo_series : ∑ m ∈ Finset.range M, (Complex.exp (-2 * Real.pi * Complex.I * h.val * (p n i) / (q n)))^m = 0 := by
          rw [ geom_sum_eq ] <;> norm_num [ h_zeta_ne_one ];
          · rw [ ← Complex.exp_nat_mul, mul_comm, Complex.exp_eq_one_iff.mpr ⟨ -h.val, ?_ ⟩ ] <;> ring_nf ; aesop;
            rw [ Nat.cast_div ] <;> norm_num ; ring_nf;
            · by_cases hq : q n = 0 <;> by_cases hp : p n i = 0 <;> aesop;
            · exact Finset.dvd_prod_of_mem _ ( Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( show p n i < 6 * n + 2 from by
                                                                                                have := Finset.mem_filter.mp ( show p n i ∈ Finset.filter ( fun p => 5 ≤ p ∧ p.Prime ) ( Finset.range ( 6 * n + 2 ) ) from ?_ ) ; aesop;
                                                                                                exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ), by
                                                                                                have h_prime : p n i ∈ P_n n := by
                                                                                                  have h_perm : ∀ x ∈ primes_list n, x ∈ P_n n := by
                                                                                                    exact fun x hx => Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 hx
                                                                                                  exact h_perm _ ( List.get_mem _ _ );
                                                                                                exact ⟨ Finset.mem_filter.mp h_prime |>.2.1, Finset.mem_filter.mp h_prime |>.2.2 ⟩ ⟩ );
            · exact Nat.Prime.ne_zero ( by exact ( Finset.mem_filter.mp ( show p n i ∈ P_n n from by exact ( mem_P_n_iff_exists_index n _ |>.2 ⟨ i, rfl ⟩ ) ) ) |>.2.2 );
          · simpa [ neg_div, mul_assoc, mul_comm, mul_left_comm ] using h_zeta_ne_one;
        exact Eq.trans ( Finset.sum_congr rfl fun _ _ => by rw [ ← Complex.exp_nat_mul ] ; ring_nf ) h_geo_series;
      -- Split the sum into residue classes modulo $p_i$.
      have h_split_sum : ∑ x : ZMod (q n), (if (x.cast : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (x.cast : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then (Complex.exp (-2 * Real.pi * Complex.I * (h.val * x.val : ℕ) / (q n : ℂ))) else 0) = ∑ r ∈ Finset.range (p n i), (∑ m ∈ Finset.range (q n / (p n i)), Complex.exp (-2 * Real.pi * Complex.I * (h.val * (r + m * (p n i)) : ℕ) / (q n : ℂ))) * (if (r : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (r : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then 1 else 0) := by
        -- We can split the sum into residue classes modulo $p_i$.
        have h_split_sum : Finset.sum (Finset.range (q n)) (fun x => if (x : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (x : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then (Complex.exp (-2 * Real.pi * Complex.I * (h.val * x : ℕ) / (q n : ℂ))) else 0) = Finset.sum (Finset.range (p n i)) (fun r => Finset.sum (Finset.range (q n / (p n i))) (fun m => if (r + m * (p n i) : ZMod (p n i)) = (r_K n i : ZMod (p n i)) ∨ (r + m * (p n i) : ZMod (p n i)) = -(r_K n i : ZMod (p n i)) then (Complex.exp (-2 * Real.pi * Complex.I * (h.val * (r + m * (p n i)) : ℕ) / (q n : ℂ))) else 0)) := by
          have h_split_sum : Finset.range (q n) = Finset.biUnion (Finset.range (p n i)) (fun r => Finset.image (fun m => r + m * (p n i)) (Finset.range (q n / (p n i)))) := by
            ext x;
            simp +zetaDelta at *;
            constructor;
            · intro hx;
              use x % (p n i), Nat.mod_lt _ (Nat.Prime.pos (by
              have h_prime : p n i ∈ P_n n := by
                exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
              exact Finset.mem_filter.mp h_prime |>.2.2)), x / (p n i), Nat.div_lt_of_lt_mul <| by
                rw [ Nat.mul_div_cancel' ];
                · linarith;
                · exact Finset.dvd_prod_of_mem _ ( by simp +decide [ mem_P_n_iff_exists_index ] )
              generalize_proofs at *;
              rw [ Nat.mod_add_div' ];
            · rintro ⟨ a, ha, b, hb, rfl ⟩ ; nlinarith [ Nat.div_mul_le_self ( q n ) ( p n i ) ];
          rw [ h_split_sum, Finset.sum_biUnion ];
          · exact Finset.sum_congr rfl fun x hx => by rw [ Finset.sum_image ( by intros a ha b hb hab; nlinarith [ Finset.mem_range.mp hx, Finset.mem_range.mp ha, Finset.mem_range.mp hb ] ) ] ; norm_cast;
          · intros r hr s hs hrs; simp_all +decide [ Finset.disjoint_left ] ;
            intro a ha x hx H; exact hrs <| by nlinarith [ show a = x from by nlinarith ] ;
        convert h_split_sum using 1;
        · refine' Finset.sum_bij ( fun x hx => x.val ) _ _ _ _ <;> simp;
          · exact fun x => ZMod.val_lt x;
          · intro a₁ a₂ h; haveI := Fact.mk ( show 1 < q n from ?_ ) ; exact ZMod.val_injective _ h;
            refine' lt_of_lt_of_le _ ( Finset.prod_le_prod' fun x hx => Nat.Prime.two_le <| Finset.mem_filter.mp hx |>.2.2 ) ; norm_num [ q ];
            exact Finset.Nonempty.ne_empty ⟨ 5, Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr ( by linarith [ show n > 0 from Nat.pos_of_ne_zero ( by rintro rfl; exact absurd ( Fin.is_lt i ) ( by simp +decide [ show w 0 = 0 from by simp +decide ] ) ) ] ), by norm_num, by norm_num ⟩ ⟩;
          · exact fun b hb => ⟨ b, ZMod.val_cast_of_lt hb ⟩;
        · simp +decide [ Finset.sum_ite ];
      -- Factor out the common term $\exp(-2\pi i h r / q)$ from the inner sum.
      have h_factor : ∀ r ∈ Finset.range (p n i), ∑ m ∈ Finset.range (q n / (p n i)), Complex.exp (-2 * Real.pi * Complex.I * (h.val * (r + m * (p n i)) : ℕ) / (q n : ℂ)) = Complex.exp (-2 * Real.pi * Complex.I * (h.val * r : ℕ) / (q n : ℂ)) * ∑ m ∈ Finset.range (q n / (p n i)), Complex.exp (-2 * Real.pi * Complex.I * h.val * m * (p n i) / (q n)) := by
        intro r hr; rw [ Finset.mul_sum _ _ _ ] ; congr; ext m; push_cast; rw [ ← Complex.exp_add ] ; ring_nf;
      simp_all +decide [ Finset.sum_ite ];
      convert congr_arg ( fun x : ℂ => ( 1 / ( q n : ℂ ) ) * x ) h_split_sum using 1;
      · unfold g_hat; norm_num [ Finset.sum_ite ] ;
        unfold g ;
        exact Or.inl ( by rw [ Finset.sum_filter ] ; congr; ext; aesop );
      · rw [ Finset.sum_eq_zero fun x hx => h_factor x <| Finset.mem_range.mp <| Finset.mem_filter.mp hx |>.1 ] ; norm_num

/-
Lemma 6.2 (The Zero-Frequency Main Term):
Using Lemma 5.3 ($q \hat{W}(0) = S_1(n)$) and Lemma 6.1 at $k=0$, prove that the $h=0$ component of the weighted hit mass evaluates exactly to the weighted interval mass multiplied by the sum of the local densities:
$$ q \sum_{i=1}^w \hat{W}(0) \overline{\hat{g}_i(0)} = S_1(n) \sum_{i=1}^w \frac{2}{p_i} $$
-/
lemma zero_frequency_main_term (n : ℕ) (hn : n ≥ 1) (W : ZMod (q n) → ℝ)
    (h_supp : ∀ x : ZMod (q n), x.val ∉ A_n n → W x = 0) :
    (q n : ℂ) * ∑ i : Fin (w n), W_hat n W 0 * starRingEnd ℂ (g_hat n i 0) =
    (S_1 n W : ℂ) * ∑ i : Fin (w n), ((2 : ℝ) / (p n i : ℝ) : ℂ) := by
      -- By Lemma 6.1 (specifically `dirac_comb_nonzero` with $k=0$), $\hat{g}_i(0) = \frac{2}{p_i} \cos(0) = \frac{2}{p_i}$.
      have h_g_hat_zero : ∀ i : Fin (w n), g_hat n i 0 = (2 / (p n i : ℝ) : ℝ) := by
        intro i
        have := dirac_comb_nonzero n i ⟨0, by
          have h_prime : ∀ i : Fin (w n), 5 ≤ p n i := by
            intro i
            have h_prime : p n i ∈ P_n n := by
              have h_perm : ∀ x ∈ primes_list n, x ∈ P_n n := by
                exact fun x hx => Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 hx;
              exact h_perm _ ( List.get_mem _ _ )
            exact (Finset.mem_filter.mp h_prime).right.left
          exact lt_of_lt_of_le (by norm_num) (h_prime i)⟩
        generalize_proofs at *; (
        aesop);
      -- By Lemma 5.3 (specifically `compact_support_equivalence`), $S_1(n) = q \hat{W}(0)$.
      have h_S1 : S_1 n W = (q n : ℝ) * (W_hat n W 0).re := by
        exact compact_support_equivalence n hn W h_supp;
      simp_all +decide [ Complex.ext_iff, Finset.mul_sum _ _ _ ];
      unfold W_hat; norm_num [ Finset.mul_sum _ _ _, Finset.sum_mul _ _ _, mul_assoc ] ;

/-
Theorem 6.3 (The Resonant Sieve Equation):
By splitting the sum in Lemma 5.4 into $h=0$ and $h \ne 0$, and applying Lemma 6.1 and 6.2, establish the exact equation for the weighted hit mass $S_2(n)$:
$$ S_2(n) = S_1(n) \sum_{i=1}^w \frac{2}{p_i} + q \sum_{i=1}^w \sum_{k=1}^{p_i-1} \hat{W}\left(k \frac{q}{p_i}\right) \frac{2}{p_i} \cos\left( \frac{2\pi k r^K_i}{p_i} \right) $$
-/
theorem resonant_sieve_equation (n : ℕ) (hn : n ≥ 1) (W : ZMod (q n) → ℝ)
    (h_supp : ∀ x : ZMod (q n), x.val ∉ A_n n → W x = 0) :
    (S_2 n W : ℂ) = (S_1 n W : ℂ) * ∑ i : Fin (w n), ((2 : ℝ) / (p n i : ℝ) : ℂ) +
    (q n : ℂ) * ∑ i : Fin (w n), ∑ k ∈ (Finset.range (p n i)).erase 0,
      W_hat n W (((k * (q n / p n i)) : ℕ) : ZMod (q n)) *
      ((2 : ℝ) / (p n i : ℝ) * Real.cos (2 * Real.pi * (k : ℝ) * (r_K n i : ℝ) / (p n i : ℝ)) : ℂ) := by
        have := @plancherel_hit_expansion n hn W h_supp;
        -- Split the sum over $h \in \mathbb{Z}/q\mathbb{Z}$ into $h$ where $\hat{g}_i(h) \neq 0$ and $h$ where $\hat{g}_i(h) = 0$.
        have h_split : ∀ i : Fin (w n), ∑ h : ZMod (q n), W_hat n W h * starRingEnd ℂ (g_hat n i h) = ∑ k ∈ Finset.range (p n i), W_hat n W (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n)) * starRingEnd ℂ (g_hat n i (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n))) := by
          intro i
          have h_split : ∀ h : ZMod (q n), g_hat n i h ≠ 0 → ∃ k ∈ Finset.range (p n i), h = (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n)) := by
            intro h hh; contrapose! hh; simp_all ;
            convert dirac_comb_zero n i h _ ; aesop;
          rw [ ← Finset.sum_subset ( Finset.subset_univ ( Finset.image ( fun k : ℕ => ( k * ( q n / p n i ) : ℕ ) : ℕ → ZMod ( q n ) ) ( Finset.range ( p n i ) ) ) ) ];
          · rw [ Finset.sum_image ];
            intros a ha b hb hab;
            have h_eq : (a * (q n / p n i) : ℕ) ≡ (b * (q n / p n i) : ℕ) [MOD q n] := by
              simp_all +decide [ ← ZMod.natCast_eq_natCast_iff ];
            have h_eq : a ≡ b [MOD p n i] := by
              rw [ Nat.modEq_iff_dvd ] at *;
              obtain ⟨ k, hk ⟩ := h_eq;
              norm_num +zetaDelta at *;
              exact ⟨ k, by nlinarith [ Nat.div_mul_cancel ( show p n i ∣ q n from Finset.dvd_prod_of_mem _ <| Finset.mem_filter.mpr ⟨ Finset.mem_range.mpr <| show p n i < 6 * n + 2 from by
                                                                                                                                                                have h_pi_lt : p n i ∈ P_n n := by
                                                                                                                                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                                                                                                                                exact Finset.mem_range.mp ( Finset.mem_filter.mp h_pi_lt |>.1 ), by
                                                                                                                                                                have h_prime : p n i ∈ P_n n := by
                                                                                                                                                                  exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ );
                                                                                                                                                                exact ⟨ Finset.mem_filter.mp h_prime |>.2.1, Finset.mem_filter.mp h_prime |>.2.2 ⟩ ⟩ ), show ( q n : ℤ ) > 0 from Nat.cast_pos.mpr <| Finset.prod_pos fun p hp => Nat.Prime.pos <| Finset.mem_filter.mp hp |>.2.2 ] ⟩;
            exact Nat.mod_eq_of_lt ( Finset.mem_range.mp ha ) ▸ Nat.mod_eq_of_lt ( Finset.mem_range.mp hb ) ▸ h_eq;
          · simp +zetaDelta at *;
            exact fun x hx => Or.inr <| Classical.not_not.1 fun hx' => by obtain ⟨ k, hk₁, hk₂ ⟩ := h_split x hx'; exact hx k hk₁ <| hk₂.symm;
        -- Apply the results from Lemma 6.1 to simplify the expression.
        have h_simplify : ∀ i : Fin (w n), ∑ k ∈ Finset.range (p n i), W_hat n W (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n)) * starRingEnd ℂ (g_hat n i (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n))) = (W_hat n W 0) * (2 / (p n i : ℝ)) + ∑ k ∈ Finset.erase (Finset.range (p n i)) 0, W_hat n W (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n)) * (2 / (p n i : ℝ) * Real.cos (2 * Real.pi * (k : ℝ) * (r_K n i : ℝ) / (p n i : ℝ))) := by
          intro i
          have h_simplify : ∀ k ∈ Finset.range (p n i), starRingEnd ℂ (g_hat n i (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n))) = if k = 0 then (2 / (p n i : ℝ)) else (2 / (p n i : ℝ) * Real.cos (2 * Real.pi * (k : ℝ) * (r_K n i : ℝ) / (p n i : ℝ))) := by
            intro k hk
            have h_g_hat : g_hat n i (((k : ℕ) * (q n / p n i) : ℕ) : ZMod (q n)) = if k = 0 then (2 / (p n i : ℝ)) else (2 / (p n i : ℝ) * Real.cos (2 * Real.pi * (k : ℝ) * (r_K n i : ℝ) / (p n i : ℝ))) := by
              convert dirac_comb_nonzero n i ⟨ k, Finset.mem_range.mp hk ⟩ using 1;
              split_ifs <;> norm_num [ ‹_› ];
            rw [ h_g_hat, Complex.conj_ofReal ];
          rw [ Finset.sum_congr rfl fun x hx => by rw [ h_simplify x hx ] ] ; norm_num [ Finset.sum_ite, Finset.filter_ne' ] ; ring_nf;
          rw [ Finset.sum_eq_add_sum_diff_singleton ( Finset.mem_range.mpr ( Nat.pos_of_ne_zero ( by
            exact Nat.Prime.ne_zero ( by have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by exact ( mem_P_n_iff_exists_index n ( p n i ) ) |>.2 ⟨ i, rfl ⟩ ) |>.2.2; aesop ) : p n i ≠ 0 ) ) ) ] ; norm_num ; ring_nf
          generalize_proofs at *;
          rw [ Finset.sdiff_singleton_eq_erase ] ; congr ; ext ; ring_nf;
          split_ifs <;> simp +decide [ *, mul_assoc ];
        have h_zero_frequency : (S_1 n W : ℂ) = (q n : ℂ) * W_hat n W 0 := by
          convert compact_support_equivalence n hn W h_supp using 1;
          unfold W_hat; norm_num [ Complex.ext_iff ] ;
        rw [this, Finset.mul_sum];
        simp +decide only [h_split, h_simplify, Finset.mul_sum _ _ _];
        simp +decide [ Finset.mul_sum _ _ _, mul_add, mul_assoc, mul_left_comm, Finset.sum_add_distrib, h_zero_frequency ]

/-
Lemma 7.1 (The Exact Krafft Cosine):
Using the definition $r^K_i = \lfloor(p_i+1)/6\rfloor$, admit that for all primes $p_i \ge 5$, the cosine at the third harmonic evaluates exactly to the negative cosine of a microscopic angle:
$$ \cos\left( \frac{2\pi \cdot 3 \cdot r^K_i}{p_i} \right) = -\cos\left( \frac{\pi}{p_i} \right) $$
-/
lemma exact_krafft_cosine (n : ℕ) (i : Fin (w n)) :
    Real.cos (2 * Real.pi * 3 * (r_K n i : ℝ) / (p n i : ℝ)) = -Real.cos (Real.pi / (p n i : ℝ)) := by
      -- By definition of $r_K$, we know that $p_i = 6r_K - 1$ or $p_i = 6r_K + 1$ for each $i$.
      have h_prime_cases : ∀ i : Fin (w n), p n i = 6 * r_K n i - 1 ∨ p n i = 6 * r_K n i + 1 := by
        intro i
        unfold r_K
        have h_prime : p n i ≥ 5 ∧ p n i % 6 = 1 ∨ p n i ≥ 5 ∧ p n i % 6 = 5 := by
          have h_prime : p n i ≥ 5 ∧ p n i % 2 = 1 ∧ p n i % 3 ≠ 0 := by
            have h_prime : p n i ≥ 5 ∧ Nat.Prime (p n i) := by
              have h_prime : p n i ∈ P_n n := by
                convert Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) using 1
              generalize_proofs at *; (
              exact ⟨ Finset.mem_filter.mp h_prime |>.2.1, Finset.mem_filter.mp h_prime |>.2.2 ⟩)
            generalize_proofs at *; (
            exact ⟨ h_prime.1, h_prime.2.eq_two_or_odd.resolve_left ( by linarith ), fun h => by have := Nat.dvd_of_mod_eq_zero h; rw [ h_prime.2.dvd_iff_eq ] at this <;> linarith ⟩)
          generalize_proofs at *; (
          exact if h : p n i % 6 = 1 then Or.inl ⟨ h_prime.1, h ⟩ else Or.inr ⟨ h_prime.1, by omega ⟩ ;)
        generalize_proofs at *; (
        omega;);
      obtain h | h := h_prime_cases i <;> rw [ h ] <;> ring_nf <;> norm_num [ sub_eq_add_neg ];
      · rcases k : r_K n i with ( _ | k ) <;> push_cast [ k ] <;> ring_nf <;> norm_num at *;
        · have := Finset.mem_filter.mp ( show p n i ∈ Finset.filter ( fun p => 5 ≤ p ∧ p.Prime ) ( Finset.range ( 6 * n + 2 ) ) from by
                                          exact Finset.mem_sort ( α := ℕ ) ( · ≤ · ) |>.1 ( List.get_mem _ _ ) ) ; aesop;
        · rw [ ← Real.cos_sub_pi ] ; ring_nf;
          convert Real.cos_periodic _ using 2 ; nlinarith [ Real.pi_pos, mul_inv_cancel₀ ( by linarith : ( 5 : ℝ ) + ↑‹ℕ› * 6 ≠ 0 ) ];
      · rw [ ← Real.cos_pi_sub ] ; congr ; nlinarith [ Real.pi_pos, mul_inv_cancel₀ ( by linarith [ show ( r_K n i : ℝ ) ≥ 0 by positivity ] : ( 1 + r_K n i * 6 : ℝ ) ≠ 0 ) ] ;

/-
Lemma 7.2 (The Third Harmonic Extraction):
Admit that for a strategically chosen weight function $W(x)$ where $\hat{W}(h)$ is concentrated strictly at $h=0$ and the third harmonics $h = 3q/p_i$, the Resonant Sieve Equation simplifies to:
$$ S_2(n) = S_1(n) \sum_{i=1}^w \frac{2}{p_i} - q \sum_{i=1}^w \hat{W}\left(3 \frac{q}{p_i}\right) \frac{2}{p_i} \cos\left( \frac{\pi}{p_i} \right) $$
-/
lemma third_harmonic_extraction (n : ℕ) (hn : n ≥ 1) (W : ZMod (q n) → ℝ)
    (h_supp : ∀ x : ZMod (q n), x.val ∉ A_n n → W x = 0)
    (h_spectral : ∀ i : Fin (w n), ∀ k ∈ (Finset.range (p n i)).erase 0, k ≠ 3 →
      W_hat n W (((k * (q n / p n i)) : ℕ) : ZMod (q n)) = 0) :
    (S_2 n W : ℂ) = (S_1 n W : ℂ) * ∑ i : Fin (w n), ((2 : ℝ) / (p n i : ℝ) : ℂ) -
    (q n : ℂ) * ∑ i : Fin (w n), W_hat n W (((3 * (q n / p n i)) : ℕ) : ZMod (q n)) *
    ((2 : ℝ) / (p n i : ℝ) * Real.cos (Real.pi / (p n i : ℝ)) : ℂ) := by
      rw [ resonant_sieve_equation n hn W h_supp ];
      rw [ Finset.sum_congr rfl fun i hi => Finset.sum_eq_single 3 ?_ ?_ ];
      · -- Apply the trigonometric identity $\cos(2\pi \cdot 3 \cdot r / p) = -\cos(\pi / p)$ to each term in the sum.
        have h_cos_identity : ∀ i : Fin (w n), Complex.cos (2 * Real.pi * 3 * (r_K n i : ℝ) / (p n i : ℝ)) = -Complex.cos (Real.pi / (p n i : ℝ)) := by
          intro i
          have := exact_krafft_cosine n i
          norm_cast at *;
        simp_all +decide [ Finset.mul_sum _ _ _, mul_assoc, mul_left_comm, sub_eq_add_neg ];
      · grind;
      · intro h;
        norm_num +zetaDelta at *;
        have := Finset.mem_filter.mp ( show p n i ∈ P_n n from by { exact ( mem_P_n_iff_exists_index n _ ) |>.2 ⟨ i, rfl ⟩ } ) ; norm_num at this ; interval_cases p n i <;> norm_num at *;

/-
Definition of the Krafft Admissibility condition: existence of a weight function W such that S_2(n) < S_1(n).
-/
def Krafft_Admissibility (n : ℕ) : Prop :=
  ∃ W : ZMod (q n) → ℝ, (∀ x, W x ≥ 0) ∧
  (∀ x : ZMod (q n), x.val ∉ A_n n → W x = 0) ∧
  S_2 n W < S_1 n W

/-
Theorem 7.3 (The Krafft Sieve Guarantee):
Admit that there exists a valid non-negative weight function $W(x)$ supported on $\mathcal{A}_n$ such that the magnitude of the negative third-harmonic resonance strictly overpowers the main term, yielding:
$$ S_2(n) < S_1(n) $$
Conclude that by Theorem 4.5 and the Additive Sieve Isomorphism (Lemma 1.3), a Twin Prime index is unconditionally guaranteed in $\mathcal{A}_n$.
-/
theorem krafft_sieve_guarantee (n : ℕ) (hn : n ≥ 1) (h_admit : Krafft_Admissibility n) :
    ∃ x ∈ A_n n, Nat.Prime (6 * x - 1) ∧ Nat.Prime (6 * x + 1) := by
      obtain ⟨ W, hW_nonneg, hW_supp, hW_ineq ⟩ := h_admit;
      obtain ⟨ x, hx ⟩ := weighted_existence_principle n W hW_nonneg hW_ineq;
      exact ⟨ x, hx.1, additive_sieve_isomorphism n hn x ( hx.1 ) |>.1 hx.2.2 ⟩
